# Vex — แผนพัฒนา ส.ค. 2026 (เช็คลิสต์)

โจทย์จากเจ้าของ (4 ส.ค. 2026): ความจำสั้น · แจ้งงานไม่เป็นระเบียบ · ตีความคำสั่งพลาด · อ่านลิงก์/ไฟล์ไม่ลึก · คุมโซเชียล/เครื่องไม่ได้จริง · งานเบื้องหลังเงียบหาย

หลักการที่เจ้าของสั่ง: **ไม่ฟิกข้อมูล/ไม่ฟิกรูปแบบคำสั่ง — ให้ยืดหยุ่นอิสระเหมือนคุยกับ AI ทั่วไป เพียงแต่สวมบทเลขา**

การตัดสินใจที่ได้คำตอบแล้ว:
- โซเชียล = ทาง A (Chrome ตัวจริงของเจ้าของ + remote debugging port)
- เก็บลิงก์/รูปเข้าคลัง = **เฉพาะที่สั่งให้เก็บเท่านั้น** (ไม่สั่ง = อ่านให้ ตอบให้ แต่ไม่เขียนลงคลัง)
- โพสต์/ตอบโซเชียลแทน = ยืนยันทุกครั้ง + **แคปหน้าจอยืนยันว่าอยู่ที่โพสต์นั้นจริงและพิมพ์ค้างไว้พร้อมส่ง**

---

## เฟส 1 — ฐานราก

- [x] 1.1 Router ใหม่ (`kiki-router.ts`) — เลิกพึ่ง regex ตัดสินใจ, ใช้ตัวจัดเส้นทางด้วย AI + ปิดช่อง WISH_RE hijack
- [x] 1.2 กระดานงาน `KikiTask` (`kiki-tasks.ts`) — จด/ลิสต์/ปิด/แก้ + งานมีเงื่อนไข (trigger) + ตามเตือน
- [x] 1.3 ความจำ 4 ชั้น (`kiki-memory.ts`) — index แชททั้งหมด + สรุปรายวัน + ขยายหน้าต่าง + recall อัตโนมัติ
- [x] 1.4 ตัวจัดรูปแบบกลาง (`kiki-format.ts`) + เลิกซอยบับเบิลมั่ว + เขียนบรีฟเช้าใหม่
- [x] 1.5 heartbeat 3 นาที (Hermes + งานพัฒนา) + ปุ่มยกเลิกงาน

## เฟส 2 — รับข้อมูลเข้า

- [x] 2.1 ท่ออ่านลิงก์/ไฟล์ลึก + เบราว์เซอร์สำรองเมื่อ fetch ไม่ได้เนื้อ (X/IG/FB)
- [x] 2.2 `KikiMedia` — เก็บรูป/วิดีโอ **เมื่อสั่ง** + บรรยายด้วยวิชัน + ค้นคืนด้วยความหมาย
- [x] 2.3 ลิสต์รายการเงิน "รอระบุ" ตามสั่ง + ตอบเป็นชุดทีเดียวหลายรายการ

## เฟส 3 — ลงมือแทนได้จริง

- [x] 3.1 สะพาน Chrome ตัวจริง (CDP) + อ่านโพสต์ X/IG/FB + แคปภาพ
- [x] 3.2 ตอบ/โพสต์/DM แทนบัญชี — ร่าง → พิมพ์ค้าง → แคปยืนยัน → กดส่ง
- [x] 3.3 คุม Mac ให้ทำได้จริง + ระบบยืนยันผลแทน AI (มีไฟล์จริงถึงบอกว่าทำแล้ว)

## เฟส 4 — เชิงรุก

- [x] 4.1 โหมดที่ปรึกษาการเงินโหด + รีวิวรายสัปดาห์เทียบแผน

---

---

## Vex เข้า Discord — เลขาเสียงเปิดค้างทั้งวัน (เริ่ม 4 ส.ค. 2026)

โจทย์: Vex ตัวเดิม สมองเดิม ความจำเดิม ต้องมีอีกหนึ่งช่องทางคือ Discord · เจ้าของเปิดห้องเสียงค้างไว้ทั้งวันบนมือถือแล้วดับจอ คุยด้วยปาก และ Vex ต้องพูดขึ้นมาเองได้เมื่อมีเรื่องที่ควรรู้

หลักการที่ห้ามละเมิด: สมองเดียว ท่อหลายทาง (`/api/kiki/ingest` เป็นสมองเดียว ฝั่ง Discord เป็นแค่ท่อ) · ห้ามสร้าง DB/vault ที่สอง · เชิงรุกทุกตัวเริ่มจากปิดไว้ก่อน · ไม่แน่ใจ = เงียบ · ห้ามทำให้ Telegram พัง

| เฟส | งาน | สถานะ |
|---|---|---|
| -1 | ผ่า `ingest/route.ts` (1,590 บรรทัด) เป็นทะเบียนตัวจัดการ | **เสร็จ 4 ส.ค. 2026** |
| 0 | เสียงเทียบให้เลือก | **อัดครบส่งให้ฟังแล้ว 4 ส.ค. — รอเจ้าของเลือก** (มีแต่ key Gemini เจ้าอื่นรอสมัคร) |
| 0.5 | ตัวตนเจ้าของหลายบัญชี + ประวัติข้ามช่องทาง | **เสร็จ 4 ส.ค. 2026** |
| 1 | ท่อ Discord ฝั่งข้อความ | **เสร็จ 4 ส.ค. 2026** |
| 2 | บอทเข้าห้องเสียง + ชั้นเลือกปลายทางแจ้งเตือน | **เสร็จ 4 ส.ค. 2026** |
| 3 | เสียงขาเข้า (VAD → STT → ingest → ตอบเสียง) | **เสร็จ 4 ส.ค. 2026** |
| 4 | ตา — userbot listener + ตัวให้คะแนนขัดจังหวะ + focus stack | **เสร็จ 4 ส.ค. 2026** |
| 5 | มือ — ลูปตอบกลับ + โปรไฟล์สไตล์ + ระดับความไว้ใจ | **เสร็จ 4 ส.ค. 2026** |
| 6 | 4 โหมดฟัง + คำปลุก + หยุดกลางประโยค | **เสร็จ 4 ส.ค. 2026** |
| 15 | งานเบื้องหลังหลายเรื่องพร้อมกัน + ถามสถานะได้ + คิวจำกัด 2-3 งาน | **เสร็จ 4 ส.ค. 2026** |

### เฟส 0.5 เสร็จแล้ว — ตัวตนเดียว ผูกได้หลายบัญชี

**สิ่งที่พบตอนสำรวจ (ทำให้งานเล็กลงกว่าที่คิด)**
- `KikiChat` ไม่มีคอลัมน์ `chatId` มาแต่แรก → ประวัติเป็นสายเดียวรวมกันอยู่แล้ว ทักช่องไหนก็ต่อเรื่องเดิมได้ทันที ไม่ต้องรื้ออะไร
- `getKikiOwnerId()` มีจุดเรียกจริงแค่ 3 จุด ทั้งหมดอยู่ใน ingest

**ที่ทำจริง**
- `KikiChat.channel` (`telegram` | `discord` | `discord-voice` | `cron`) ดีฟอลต์ `telegram` — แถวเก่า 530 แถวได้ค่าครบ ไม่ต้อง backfill
- `kiki.ts`: `ownerAccounts()` / `isOwnerAccount()` / `linkOwnerAccount()` / `unlinkOwnerAccount()` เก็บที่ Setting `kiki_owner_accounts` = `["telegram:<id>", "discord:<id>"]` · seed อัตโนมัติจาก `kiki_owner_id` เดิมตอนถูกเรียกครั้งแรก · `getKikiOwnerId()` คงไว้ ของเดิมไม่พัง
- `issueLinkCode()` / `redeemLinkCode()` — รหัส 4 หลัก อายุ 10 นาที ใช้ครั้งเดียว ไม่มีเลขซ้ำติดกัน (อ่านออกเสียงได้ ไว้ผูกด้วยปากในเฟส 6)
- **เอา auto-bind "คนแรกที่ทัก = เจ้าของถาวร" ออก** เหลือเฉพาะกรณีระบบยังไม่เคยมีเจ้าของเลย **และ** มาจาก Telegram เท่านั้น → ช่องทางใหม่ตั้งตัวเองเป็นเจ้าของไม่ได้
- ช่องทางที่ยังไม่ผูก ตอบได้อย่างเดียวคือผลของการกรอกรหัส นอกนั้นเงียบสนิท
- ล็อกโหมดกลุ่มเทรนเนอร์อั๋นไว้เฉพาะ `platform === "telegram"` — กัน chatId ช่องทางอื่นชนแล้วทำ scope `aun` รั่ว
- `kikiConversation()` ติดป้ายสื่อเฉพาะช่องทางที่ไม่ใช่ telegram (`เจ้าของ (พูดในสาย Discord): ...`) ประวัติเดิมหน้าตาไม่เปลี่ยน

**เทสจริง 15 ข้อ ผ่านหมด** (ยิงเข้า `/api/kiki/ingest` ของจริง แล้วลบข้อมูลเทสออกทั้ง DB และเวกเตอร์)
- คนอื่นทักทาง Telegram / Discord = เงียบสนิท ไม่ถูกตั้งเป็นเจ้าของ · เดารหัสมั่วไม่ติด · รหัสผิดไม่ผูก · รหัสเดิมใช้ซ้ำไม่ได้
- พูดใน Discord ให้จำ → ถามต่อใน Telegram ตอบได้ และ Vex อ้างเองว่า "พี่บอกไว้เมื่อกี้ในสายเสียง Discord"
- Telegram เดิมครบ: การ์ดการเงิน · ตารางนัด · กระดานงาน · ความจำ · กฎที่สอนไว้ · คุยธรรมดา

### เฟส 3-6 + ข้อ 15 เสร็จแล้ว — ครบทุกเฟส (4 ส.ค. 2026)

**เสียงขาเข้า (เฟส 3)** — ด่านกรอง 4 ชั้นซ้อนกัน กันทั้งเงินและการแทรกผิดจังหวะ
1. Discord VAD (เงียบ = ไม่มีเสียงส่งมาเลย ฟรี) → 2. ตัดประโยคเมื่อเงียบ 900ms →
3. ทิ้งประโยคสั้นกว่า 0.6 วิ → 4. เพดาน 25 ครั้ง/นาที (กันทีวี/คลิปเปิดค้างยิง STT รัว)
ถอด Opus ด้วย `opusscript` (pure JS ไม่ต้องคอมไพล์) → ffmpeg → OGG 16k → Gemini STT

**4 โหมดฟัง (เฟส 6)** — เรียกชื่อ (ค่าเริ่มต้น) · อิสระ · ฟังเงียบ · ปิดปาก · สั่งเปลี่ยนด้วยปาก
หน้าต่างคุย 30 วิ ยืดทุกประโยค · ปลุกผิดหลับต่อเงียบ ๆ ไม่ถามว่า "มีอะไรครับ" ·
เสียงสัญญาณสร้างเองด้วย ffmpeg (ตุ๊บ/สองโทนขึ้น/สองโทนลง) เล่นแทรกได้ทันทีไม่ต้องรอ TTS

**งานเบื้องหลัง (ข้อ 15)** — ประเมินเองว่าเกิน 10 วิ → ตอบรับก่อนแล้วไปทำเบื้องหลัง
เพดาน 2 งานพร้อมกัน ที่เหลือเข้าคิวและบอกว่ารอคิวอยู่ · ถามสถานะได้ · กระดานเรื่องที่ค้าง
แยกงานที่ฝาก Hermes ได้/ไม่ได้ (ต้องใช้ความจำ/เงิน/นัด/คลัง = ทำในตัว Vex ห้ามส่งออก)

**ตา (เฟส 4)** — `vex-eyes.mjs` ดักข้อความ Telegram ขาเข้า → ท่อเหตุการณ์กลาง
ตัวให้คะแนน 3 แกน (ด่วน · ใครพูด · ขัดจังหวะแพงไหม — อ่านปฏิทินจริง+เวลา+อยู่ในสายไหม)
→ 4 ระดับ · กันซ้ำ 20 นาที · เว้นระยะขั้นต่ำ 90 วิ · **ตัวเฝ้าห้ามตัดสินใจพูดเอง**

**มือ (เฟส 5)** — เรียนสไตล์การคุยรายคนจากประวัติแชทจริง (ไม่ให้เจ้าของมานั่งสอน)
ความไว้ใจ 0/1/2 เริ่มทุกคนที่ 1 · ถอนได้ใน 30 วิ (ลบข้อความจริงใน Telegram)
จำกัด 20 ข้อความ/ชม. + เว้น 8 วิ กันบัญชีโดนแบน · หาคนไม่เจอ/กำกวม = ถามกลับ ห้ามเดา

**บั๊ก 2 ตัวที่เจอจากการใช้จริง (ไม่ใช่จากเทส)**
1. `\b` ของ JS ใช้กับภาษาไทยไม่ได้ (อักษรไทยนับเป็น non-word ทั้งหมด) →
   **"Vex พอ" สั่งหยุดไม่ติดเลย** ทั้งที่เป็นคำสั่งสำคัญที่สุด
   แก้เป็น lookahead `(?![\u0E01-\u0E4F])` · เทส 21 เคสผ่าน รวม "พอดีเลย/พอใจ/หยุดรถ" ที่ต้องไม่ติด
2. อ่าน log ของจริงวันแรกเจอว่า **เสียงทีวีที่บังเอิญมีคำว่า "Vex" ปลุกสำเร็จ**
   แล้วหน้าต่างคุย 30 วิ ทำให้เสียงรบกวนถัดมาถูกนับเป็นคำสั่งทั้งหมด ("ติ๊กต็อก บูม บูม")
   → หน้าต่างคุยแปลว่า "ไม่ต้องเรียกชื่อซ้ำ" **ไม่ได้แปลว่า "รับทุกเสียงที่ได้ยิน"**
   ตอนนี้ทุกประโยคต้องผ่านตัวกรอง "พูดกับเราไหม" ยกเว้นเรียกชื่อพร้อมคำสั่งในประโยคเดียว

**ข้อจำกัดที่ต้องรู้: โควตา Gemini TTS**
วันแรกใช้เสียงหนักมาก (เทียบ 30 เสียง + เทสทั้งวัน) → **TTS ตอบ HTTP 429 โควตาหมด**
สมองกับการถอดเสียงยังปกติ (คนละโควตา) แปลว่า **Vex ยังฟังและคิดได้ แต่พูดไม่ได้ชั่วคราว**
ใส่ทางถอยแล้ว: แปลงเป็นเสียงไม่ได้ = ลงห้อง `#vex` พร้อมสัญลักษณ์ 🔇 แทน ไม่ปล่อยให้คำตอบหายเงียบ
ถ้าจะใช้เสียงหนักทุกวัน ต้องเปิด billing ของ Google AI Studio (~0.76 บาท/1,000 ตัวอักษร)

### เฟส 2 เสร็จแล้ว — บอทอยู่ในห้องเสียง + ชั้นเลือกปลายทาง

**สถาปัตยกรรม: กล่องขาออก ไม่ใช่ให้ท่อยิง cron เอง**
เว็บ (ที่มีสมองกับ cron) กับท่อ Discord เป็นคนละโปรเซส และ "เจ้าของอยู่ในสายไหม" มีแต่ท่อที่รู้
→ เว็บหย่อนงานลงตาราง `VexOutbox` แล้วท่อมาหยิบไปส่งเอง (ท่อรายงาน presence กลับมาทุก 5 วิ)
ได้ของแถมที่จำเป็น: เจ้าของออกจากห้อง = ของค้างในกล่องรอจนกลับมา ไม่หายไปไหน

| ส่วน | ไฟล์ |
|---|---|
| ตัดสินปลายทาง + presence + ย่อเป็นภาษาพูด | `src/lib/kiki-outbox.ts` |
| กล่องขาออกให้ท่อมาหยิบ | `src/app/api/kiki/outbox/route.ts` |
| แปลงข้อความเป็นเสียงให้ท่อ (ท่อไม่ต้องรู้จักโมเดล) | `src/app/api/kiki/tts/route.ts` |
| เข้าห้องเสียง + คิวพูด + วนหยิบงาน | `scripts/kiki-discord.mjs` |
| รันเองตอนเปิดเครื่อง | LaunchAgent `com.changoh.vexdiscord` (KeepAlive) |

**กติกาที่บังคับไว้ในโค้ด**
- `vex_voice_announce` **ค่าเริ่มต้นปิด** — สั่งเปิดด้วยปากเท่านั้น (intent `voice_announce`)
  ปิดอยู่ = `routeProactive()` ไม่แตะ Discord เลยแม้แต่นิดเดียว
- **Telegram ได้ของครบเหมือนเดิมเสมอ** ไม่ว่าอะไรจะเกิดขึ้น — Discord เป็นแค่สำเนาที่เพิ่มเข้ามา
- **ห้ามพูดใส่ห้องเปล่า** — ไม่อยู่ในสาย = งานเสียงค้างในกล่อง ไม่นับเป็น tries ที่ล้มเหลว
  ออกจากห้องกลางประโยค = `player.stop()` ทันที
- presence เก่าเกิน 3 นาที = ถือว่าไม่อยู่ (ท่ออาจตายไปแล้ว — ดีกว่าพูดใส่ห้องเปล่า)
- พูดทับกันไม่ได้ (`speaking` lock) · เสียงหยิบทีละชิ้น ข้อความหยิบได้ทีละ 5

**เสียงที่พูดออกมาไม่ใช่ข้อความดิบ** — `toSpeech()` ย่อด้วยสมองก่อนเสมอ:
ทวนหัวเรื่องขึ้นต้น (จอดับ มองไม่เห็นว่าตอบเรื่องไหน) · 1-2 ประโยค · ห้ามอ่านลิสต์/ตัวเลขทุกตัว ·
รายละเอียดเต็มไปอยู่ในห้อง `#vex` ให้ย้อนดู

**กับดักที่เสียเวลาไปมาก: `DISCORD_GUILD_ID` ใน .env ผิด**
เป็นคนละ id กับ guild จริง (`1534194098318213182` → ที่ถูกคือ `1534194097303195688`)
อาการคือ `joinVoiceChannel` **ค้างจนหมดเวลาโดยไม่มี error บอกสาเหตุเลย** เพราะ gateway
ไม่ส่ง voice event ของ guild ที่บอทไม่ได้อยู่ → แก้ .env แล้ว **และให้ท่ออ่าน guild จากตัวห้องเอง
ไม่เชื่อค่าใน env อีก** (ผิดไม่ได้อีกแล้ว) · อีกจุด: `connectVoice()` เคยบล็อกไม่ให้ตัววนหยิบงานเริ่ม
ถ้าห้องเสียงต่อไม่ได้ ฝั่งข้อความก็ตายไปด้วย — สลับลำดับแล้ว

**เทสจริงที่ผ่าน**
- ปิดอยู่ → cron ไม่หย่อนอะไรลงกล่องเลย · สั่งเปิดด้วยภาษาคน → ค่าถูกเซ็ตจริง
- หย่อนงานเสียง+ข้อความ → ท่อหยิบไปพูดออกลำโพงจริงและโพสต์ในห้องจริง
- **เทสข้อ 9**: โด้ไม่อยู่ในห้อง → ไม่พูด ค้างในคิว · กลับเข้ามา → เล่าให้ฟัง
- **เทสข้อ 10**: เตะบอทออกจากห้องเสียงผ่าน API → กลับเข้ามาเองใน 12 วินาที

**ค้าง:** โหมดประกาศตั้งกลับเป็น "ปิด" แล้วหลังเทส (ผมเปิดเองตอนเทส ไม่ใช่เจ้าของสั่ง)
เปิดใช้จริงพูดว่า "เตือนผมด้วยเสียงด้วย" หรือ "พูดขึ้นมาเองได้เลย"

### เฟส 1 เสร็จแล้ว — ท่อ Discord ฝั่งข้อความ

`scripts/kiki-discord.mjs` (`npm run kiki:discord`) — โครงเดียวกับ `kiki-bot.mjs` เป๊ะ
เป็นแค่ท่อ ไม่มีตรรกะตัดสินใจ สมองยังเป็น `/api/kiki/ingest` ตัวเดียวกับ Telegram
ใช้ `discord.js` v14 (เจ้าของระบุไว้ในสเปกเองว่าเฟส 2 จะใช้ `@discordjs/voice` คู่กัน)

**ที่ต้องแก้ก่อนเปิดใช้จริง (หนี้จากเฟส 0.5 — จ่ายแล้ว)**
- ร่างที่ค้างรอยืนยัน 4 ตัว (`kiki_pending_dm` · `_social` · `_group` · `_dev`) เคยเป็นช่องเดียวร่วมกัน
  ค้างร่างที่ Telegram แล้วพูด "ส่งเลย" ใน Discord = **ส่งของผิดตัวออกไปในนามเจ้าของจริง ๆ**
  แก้เป็น `setPendingFor()` / `getPendingFor()` เก็บช่องทางไว้ในตัวร่าง ยืนยันข้ามช่องทางไม่ได้
  ร่างรูปแบบเก่าที่ไม่มีป้ายช่องทาง = ถือเป็นของ Telegram (ของเดิมไม่พัง)
- `msgId` ของ Discord เป็น snowflake 19 หลัก — `Number()` ทำให้ค่าเพี้ยนเพราะเกิน MAX_SAFE_INTEGER
  แล้ว reply ไปผิดข้อความ · เปลี่ยน `Send.replyTo` กับ `Ctx.msgId` เป็น `number | string` ส่งผ่านตามชนิดเดิม
- `addKikiChatId()` เรียกเฉพาะ Telegram — cron ใช้ตัวแรกในรายการนั้นเป็นปลายทาง
  ถ้าเอา channel id ของ Discord ไปปน งานเชิงรุกจะยิงผิดที่

**ที่ท่อทำ**
- แปลง HTML ของ Telegram → มาร์กดาวน์ Discord (`<b>` → `**`, `<tg-spoiler>` → `||`, `<pre>` → code block)
  แปลงที่ท่อ ไม่แตะฝั่งสมอง — Telegram จึงไม่กระทบเลย
- ซอยข้อความที่ 2,000 ตัว (Telegram 4,096 · ตัวจัดการตัดที่ 3,900) โดยพยายามตัดที่ขึ้นบรรทัดใหม่
- ปุ่ม `Send.buttons` → ปุ่มจริงของ Discord (`customId` = callbackData เดิม) กดแล้วเอาปุ่มออกกันกดซ้ำ
- ไฟล์แนบเข้า/ออกครบ: รูป · เสียง · วิดีโอ · เอกสาร → เข้า `imageFiles/audioFiles/videoFiles/docFiles` เหมือน Telegram
- "กำลังพิมพ์" ต่ออายุทุก 8 วิระหว่างรอสมอง · ลองใหม่ 3 รอบถ้าเว็บกำลังรีสตาร์ท
- ตัวตนเจ้าของผูกจาก `DISCORD_OWNER_ID` ใน `.env` โดยตรง (เจ้าของกรอกเอง = การยืนยันตัวตน ไม่ใช่ auto-bind ใครก็ได้)

**บทเรียนจากการเทส: ตรรกะถูกแต่ถ้อยคำโกหก**
ตอนเทสยืนยันข้ามช่องทาง ระบบกันไว้ถูกต้องทุกอย่าง แต่ `vexLine()` เรียบเรียงข้อความใหม่
จนกลายเป็น *"ผมส่งยืนยันให้ทาง Telegram แล้วนะครับ 📤"* — **ซึ่งไม่จริง ไม่ได้ส่งอะไรเลย**
→ ข้อความที่สื่อว่า "ยังไม่ได้ทำ" ห้ามผ่าน `vexLine()` เด็ดขาด ใส่ `canned-ok` ทุกจุด

**ตัวตรวจใหม่: เจตนาต้องมีตัวรับครบ**
`npm run kiki:check` ตรวจเพิ่มว่าทุก `id` ใน `INTENT_CATALOG` มี `is("<id>")` ในตัวจัดการจริง
เพิ่ม intent แล้วลืมเขียนตัวรับ = คำสั่งตกไปคุยเล่นเงียบ ๆ ไม่มี error ไม่มี log
เกิดจริง 2 รอบใน 1 วัน เลยยกให้เครื่องตรวจแทนสายตาคน (exit 1 = ตัวรันงานพัฒนา commit ไม่ผ่าน)

### เฟส -1 เสร็จแล้ว — ผ่า ingest เป็นทะเบียนตัวจัดการ

`route.ts` เดิม 1,590 บรรทัด: `POST` ก้อนเดียวยาว 1,440 บรรทัด มี `if (is("..."))` เรียงตายตัว 56 สาขา
แก้อะไรทีต้องไล่อ่านทั้งก้อน และแทรกพฤติกรรมต่างช่องทางไม่ได้เลยถ้าไม่แตะทุกสาขา

**โครงใหม่**

| ไฟล์ | หน้าที่ | บรรทัด |
|---|---|---|
| `route.ts` | auth → แกะ body → ถอดเสียง → โหมดอั๋น → ประตูเจ้าของ → เตรียม Ctx → เดินทะเบียน → ดักพัง | 308 |
| `types.ts` | `Send` · `Ctx` · `Handler` | 67 |
| `shared.ts` | ตัวช่วยที่หลายตัวจัดการใช้ร่วม (การ์ดการเงิน, vexSay, escHtml ฯลฯ) | 71 |
| `registry.ts` | ลำดับตัวจัดการทั้ง 56 ตัว + คอมเมนต์คู่ที่ห้ามสลับ | 128 |
| `handlers/*.ts` | ตัวงานจริง (ตอนผ่าคือ 12 ไฟล์ · ปัจจุบัน 17 — เพิ่ม eyes/files/gui/jobs/reply) | 1,583 |

**วิธีย้ายที่ใช้ (สำคัญ — ทำซ้ำได้ถ้าต้องผ่าไฟล์อื่น)**
- ใช้สคริปต์ตัดโค้ดตามช่วงบรรทัด ไม่พิมพ์ใหม่ → โค้ดที่ย้าย **เหมือนต้นฉบับทุกตัวอักษร**
- ตัวจัดการ destructure จาก `ctx` ที่หัวฟังก์ชัน → เนื้อในไม่ต้องแก้ชื่อตัวแปรสักตัว
- พิสูจน์ 3 ชั้นก่อนเชื่อ: บรรทัดครบไม่หายไม่ซ้ำ (1,188/1,214 · ที่เหลือคือ router+urls ที่ย้ายเข้า Ctx) ·
  diff ทีละตัวจัดการกับต้นฉบับ 56/56 เหมือนเป๊ะ · ลำดับในทะเบียนตรงทุกตำแหน่ง
- เทสถดถอยยิงของจริง 25 เส้นทาง ผ่านหมด แล้วลบข้อมูลเทสออกทั้ง DB และเวกเตอร์

**ซ่อมของที่พังอยู่ก่อนแล้ว**
- 4 เจตนาในแคตตาล็อกไม่มีตัวรับ ใช้ regex ดักอย่างเดียวมาตลอด — พูดคนละคำเมื่อไหร่ คำสั่งหายเงียบ
  ต่อ `voice_mode` · `finance_budget` · `journal` · `tg_chat_summary` เข้าทะเบียนแล้ว
- `voice_mode` เคยเดา "เปิดหรือปิด" ด้วย regex — เทสจริงเจอว่า "ไม่ต้องพูดแล้ว ตอบเป็นตัวหนังสือพอ"
  ไม่มีคำว่า "ปิด" เลย**กลายเป็นสั่งเปิด** แก้เป็นให้ตัวอ่านเจตนาส่ง `mode: on|off|change` มาแทน
- `npm run kiki:check` เคยลิสต์ไฟล์ตายตัว พอผ่าเป็น `handlers/` มันไม่รู้จัก กฎห้ามข้อความสำเร็จรูป
  **หยุดบังคับใช้เงียบ ๆ** (เฝ้าอยู่ 16 จุด เหลือ 2) — แก้เป็นเดินทั้งโฟลเดอร์แล้ว

### เฟส 0 — เทียบเสียง (อัดครบแล้ว รอเจ้าของเลือก)

`scripts/vex-tts-bakeoff.mjs` — สคริปต์แยกขาด ไม่ import อะไรจาก `src/` ไม่แตะ DB รันซ้ำได้

**วิธี: คัดด้วยเครื่องก่อน ให้คนฟังรอบสุดท้าย**
1. ยิงประโยคที่ยากที่สุดเข้าทุกเสียง → ถอดเสียงกลับด้วย STT → วัด 3 อย่าง
   - **คำครบ** — คำสำคัญหายไหม (โด้ / อั๋น / Telegram / invoice / EasySlip / 12,450 / บาท)
   - **อังกฤษชัด** — คำอังกฤษถอดกลับมาเป็นตัวอักษรอังกฤษไหม (ถ้าอ่านด้วยสำเนียงไทย STT จะถอดเป็นไทย)
   - **เหมือนทั้งประโยค** — Levenshtein เทียบต้นฉบับ จับกรณีเพี้ยนย่อยที่คำสำคัญยังผ่าน
2. 5 เสียงที่รอด × 3 ประโยค → ส่งเข้า Telegram ให้ฟังจากหูฟังบนมือถือ

**ผลรอบคัด 29/30 เสียง** (Vindemiatrix ยิงไม่ติดซ้ำ ๆ ด้วย error "Model tried to generate text")
- ตกรอบเพราะกินคำ: Zephyr, Fenrir (5/7) · Puck, Leda, Algieba, Despina, Laomedeia (กิน "โด้") · Alnilam (กิน "อั๋น")
- ตกรอบเพราะอ่านอังกฤษเป็นสำเนียงไทย: Aoede, Fenrir, Achernar (EasySlip กลายเป็นไทย)
- **เสียงที่ผ่านทุกตัวชี้วัด ~15 ตัว** — ในกลุ่มนี้ตัวคัดด้วยเครื่องแยกไม่ออกแล้ว (คะแนนเท่ากันหมด และสลับอันดับได้ระหว่างรอบเพราะ STT ไม่ deterministic) จากตรงนี้ต้องหูเจ้าของตัดสิน
- ที่ส่งไปให้ฟัง: Rasalgethi · Callirrhoe · Leda · Iapetus · Erinome

**ราคาจริงที่วัดได้** (อ้างอิง ai.google.dev/gemini-api/docs/pricing — คิดจากความยาวเสียงจริง × 25 tokens/วินาที)

| โมเดล | ราคา output | ต่อ 1,000 ตัวอักษร |
|---|---|---|
| `gemini-2.5-flash-preview-tts` (ที่ใช้อยู่) | $10/1M tokens | ~$0.021 (~0.76 บาท) |
| `gemini-3.1-flash-tts-preview` (ตัวใหม่) | $20/1M tokens | ~$0.044 (~1.60 บาท) |
| `gemini-2.5-pro-preview-tts` | $20/1M tokens | ~$0.044 (~1.60 บาท) |

free tier ทั้งคู่ = $0 · ส่งคู่เทียบรุ่นเก่า/ใหม่เข้า Telegram ให้ฟังแล้วว่าต่างกันคุ้มไหม

**ข้อจำกัดที่ต้องรู้:** การถอดเสียงกลับจับได้แค่คำหาย/เลขผิด/ชื่อเพี้ยน — บอกไม่ได้ว่าสำเนียงเพราะหรือไม่เพราะ

**ยังไม่ทำ:** ชั้น TTS ถอดเปลี่ยนผู้ให้บริการได้ (`speak(text) → OGG/Opus`, เลือกด้วย Setting `kiki_tts_provider`) — รอเจ้าของเลือกเสียงก่อน เขียนก่อนเลือก = เดาว่าต้องรองรับอะไร

**หนี้ที่ฝากไว้ให้เฟสถัดไป**
- `kiki_pending_dm` / `kiki_pending_social` ยังเป็นช่องเดียว ยังไม่ผูกกับช่องทาง — ต้องแก้ในเฟส 1 ก่อนเปิดใช้จริง ไม่งั้นสั่ง "ส่งเลย" ข้ามช่องทางแล้วส่งของผิดตัวออกไปในนามเจ้าของ
- `addKikiChatId()` ห้ามให้ท่อ Discord เรียก ไม่งั้น channel id ปนเข้ารายการที่ cron ใช้เป็นปลายทาง
- `sanitizeVexText()` แปลงเป็น HTML ของ Telegram — Discord ต้องมี renderer ของตัวเอง
- persona ยังเรียกเจ้าของว่า "พี่" สเปกใหม่ให้เรียก "โด้" — แก้ตอนทำเฟส 2

## บันทึกการทำงาน

**5 ส.ค. 2026 (กลางคืน) — เฟส 2-4: เส้นเสียงเข้าถึงความสามารถจริง + มารยาทการพูด**

โจทย์จากโด้หลังใช้จริง: "ยังไม่โอเค มันยังโง่อยู่ · เรียกแล้วรอเป็นนาที · ชอบพูดแทรก ·
ถามอะไรก็บอกไม่รู้ · อยากคุยได้เรียลไทม์ ตอบไม่เกิน 5 วิ"

### รากปัญหาเดียวที่อธิบายเกือบทุกอาการ

เส้นเสียงไม่ได้ต่อกับสมองจริง — มีสมองที่สองที่ไม่มีเครื่องมือสักชิ้น

```
พิมพ์  → /api/kiki/ingest → ทะเบียนตัวจัดการ 57 ตัว (ค้นเว็บ · Mac · เพลง · แชท · เงิน)
เสียง → /api/kiki/voice  → askKikiVoice() = Gemini เปล่า ๆ   ← ตรงนี้
```

ชั้นหาข้อเท็จจริง (`kiki-agent`) มีเครื่องมือครบ 8 ตัวรวมค้นเว็บมาตั้งแต่แรก
แต่ `chatFallbackHandler` มีเงื่อนไข `!voiceNote` = **พูดมาเป็นเสียงเมื่อไหร่ ข้ามการหาข้อมูลทั้งหมด**
เหตุผลตอนนั้นคือ "เสียงต้องตอบไว" ซึ่งถูก แต่แลกด้วยการตอบว่าไม่รู้ทุกเรื่อง

### เฟส 0 — หยุดเลือด

| แก้อะไร | หลักฐาน |
|---|---|
| วนตอบ "ครับ" 5 รอบ | log จริงมี `ได้ยิน: "ครับ" → say` ติดกัน 5 ครั้ง · กฎเดิม "ในสาย + สั้นกว่า 12 ตัว = คำสั่ง" |
| ตัวจับเสียงตัวเองย้อนไมค์ | เดิมข้ามทุกอย่างที่สั้นกว่า 6 ตัวอักษร |
| ประตูห้ามแทรก | ท่อรู้อยู่แล้วว่าโด้กำลังพูด แต่ไม่เคยเอามาใช้ |
| เสียงมาก่อนข้อความ | เดิมโพสต์ห้องแชทก่อนแล้วค่อยพูด |

### เฟส 1 — เสียง

ลอง F5-TTS-THAI ในเครื่อง (ฟรี ไม่จำกัด) → **ตกรอบ** โด้ฟังแล้วบอก "เสียงแตก/เพี้ยน"
วัดได้ว่าคุณภาพเต็มช้ากว่าเวลาจริง 21 เท่า (81 วิ สำหรับเสียง 3.9 วิ)

จบที่ **เปิด billing ของ Gemini ตัวเดิม** — ไม่ต้องเปลี่ยนโค้ดเลย ได้เสียง Iapetus ที่โด้เลือกเอง
วัดหลังเปิด: โควตาหายปัญหา (เดิมตันที่ 15 ครั้ง/วัน) แต่**ความเร็วไม่ดีขึ้น**
ทุกโมเดลใช้ขั้นต่ำ 5.3 วินาทีแม้ประโยคสั้นสุด = พื้นของ API เอง กดต่ำกว่านี้ไม่ได้
→ สมมติฐาน "ช้าเพราะ free tier" **ผิด** · โมเดลที่ใช้อยู่เร็วสุดแล้ว

### สไตล์การพูด — ถอดจากคลิปที่โด้ส่งมา 2 ตัว

|  | คลิป 2 | Arbor | Vex เดิม |
|---|---|---|---|
| ระดับเสียง | 174 Hz | 113 Hz | 147 Hz |
| เงียบ | 40% | 45% | 34% |
| หยุดเฉลี่ย | 0.68 วิ | 0.66 วิ | **0.43 วิ** |

**ตัวเลขจากสองคลิปตรงกัน** → "ไม่เร่งไม่รีบ" มาจากความเงียบ ไม่ใช่น้ำเสียง
และเพราะตัวแปลงเสียงอ่านตามข้อความ **จังหวะต้องเขียนลงในข้อความตั้งแต่ต้น**

ถอดคำเชื่อมมาใช้จริง: แบบว่า · อะไรอย่างเงี้ย · เออ · อืม · เนาะ · นู่นนี่
**เสียง Arbor ไม่มีใน API** (เป็นเสียงเฉพาะแอป ChatGPT) — ตรวจจากเอกสารทางการแล้ว

### เฟส 2 — เลิกตอบว่าไม่รู้

- `askKikiVoice` เรียก `gatherFacts` ได้แล้ว (ค้นเว็บ/ลิงก์/เงิน/นัด/งาน/ความจำ)
- ทิ้ง `NEEDS_LOOKUP` regex 12 คำ → ใช้ตัวจัดเส้นทางตัวเดียวกับฝั่งข้อความ
- แยกทางตามชนิดเจตนา: **อ่าน/ถาม** = ตอบในเส้นเสียง · **ลงมือทำ** = ผ่านทะเบียน 57 ตัว

ผลจริง: *"วันนี้ฝนจะตกไหมที่ขอนแก่น"* → *"โด้ครับ วันนี้ที่ขอนแก่นเนี่ย ก็คือมีโอกาสฝนตกเล็กน้อยนะครับ ประมาณ 52 เปอร์เซ็นต์เลย"*

### เฟส 3 — จังหวะเหมือนคน

- **barge-in ของจริง** — ใช้สัญญาณ "เริ่มพูด" ของ Discord ตรง ๆ ไม่ต้องรอถอดเสียง (เดิมช้าไป 3-5 วิ)
- **เคาะประตูก่อนเล่า** — เรื่องเชิงรุกที่ไม่ด่วนถามก่อนว่าว่างไหม แล้วเก็บเนื้อรอจนโด้ตอบรับ
- **แข่งกับนาฬิกา** แทนเดาจากคำ — เกิน 5 วิ = พูด "ขอเวลาแป๊บ" แล้วส่งผลตามทีหลัง

### บั๊กที่เจอระหว่างทาง (กระทบ Telegram ด้วย ไม่ใช่แค่เสียง)

ตัวจับ "319 ค่าตั๋วหนัง" ใช้ `/^[\d,]+\s+\S/` ซึ่งชนกับข้อความที่ขึ้นต้นด้วยเลขทุกแบบ
ถามด้วยเสียง "หกโมงเย็นฝนตกไหม" → STT คืน "6 โมง เย็น..." → **ถูกลากไปเข้าเส้นทางจัดหมวดเงิน**
(พิมพ์ "6 โมงเย็นนัดใคร" ก็ชนเหมือนกัน) · ตรวจแล้วข้อมูลการเงินไม่เสียหาย

### กับดักการวัดผลที่เกือบหลงเชื่อ

- **เทียบตัวเลขบนข้อมูลที่ยังขยับ** — เทียบตรรกะเก่า/ใหม่ของตัวอ่านโทเค็นแล้วต่างกัน 8 จุด
  แต่ตัวใหม่ได้เลข *มากกว่า* เสมอ เพราะเซสชันที่ทำงานอยู่เขียน log สดตลอด
  สลับลำดับการวัด → ส่วนต่างเป็น 0 ทุกค่า
- **เทสไม่ครอบคลุมแล้วคิดว่าผ่าน** — คำอนุญาต "ว่ามา" ผ่าน แต่ "ว่ามาเลย" ไม่ผ่าน
  เพราะไล่เขียนเป็นวลีเต็มทีละอัน · แก้เป็นแยกกริยากับหางประโยค
- **เรียกของช้าโดยไม่จำเป็น** — เส้นทางเล่าเรื่องเรียก `toSpeech` ที่ใช้ Claude CLI: 26-38 วินาที
  เปลี่ยนไปใช้สมองสายด่วน เหลือ 10.0 วินาที
- **ค้นข้างนอกทั้งที่ข้อมูลอยู่ในมือ** — ถาม "งานค้างมีกี่งาน" ก็ยังไปค้นเว็บ
  ทั้งที่บริบทถูกฉีดให้อยู่แล้ว → ออกไปค้นเฉพาะ web_research/shopping/มีลิงก์

### ที่ยังเหลือ

**ความเร็วยังไม่ถึง 5 วินาที** — วัดล่าสุด 9-10 วิ (ถอดเสียง 3-4 + อ่านเจตนา 1.5-2 + คิด 4)
และยังไม่รวมเวลาแปลงเป็นเสียงอีก 5-12 วิ
ทางออกที่วิเคราะห์ไว้แล้วแต่ยังไม่ได้ทำ: **สตรีมเสียง** — ซอยคำตอบเป็นประโยค
พูดประโยคแรกทันทีระหว่างที่ประโยคถัดไปกำลังทำ (ประโยคสั้น 48 ตัวใช้ 5.7 วิ แต่พูดได้ 4-5 วิ
= มีเวลาพอทำประโยคถัดไปให้ทันพอดี ต่อกันไม่มีช่องว่าง)


**5 ส.ค. 2026 (เย็น) — ก้อน B: บั๊กที่เจ้าของเจอจากการใช้จริง 3 เรื่อง**

*ทุกข้อไล่หาต้นตอจากโค้ด/ล็อกจริงก่อนแก้ ไม่ได้เดาจากอาการ*

**B1. เตือนนัดเงียบสนิทตอนถึงเวลา — บันไดเตือนขาด 3 ขั้นสุดท้าย**
`cron/route.ts` มีบันได 4 ขั้น: เย็นก่อน 18:00 → เช้า 07:00 → **ก่อนถึง 1 ชม.** → หลังนัดจบ
ขั้นที่ 3 ยิงในหน้าต่าง `>0 && <=65 นาที` แล้วเซ็ต `remindedHour=true` ทันที = **ยิงครั้งเดียวจบ**
→ ตั้งแต่ T-65 นาทีจนนัดจบ **ไม่มีอะไรออกไปเลย ไม่มีขั้น "ถึงเวลาแล้ว" อยู่ในโค้ดตั้งแต่แรก**

เพิ่ม 3 ขั้น + คอลัมน์ `remindedSoon` / `remindedNow` / `nudgedLate`:
- T-10 นาที (บีบปลายล่างของขั้น 1 ชม. เป็น 12 นาที กันนัดกระชั้นได้การ์ด "อีก 1 ชม." ที่ผิดความจริง)
- **T-0 "ถึงเวลาแล้ว"** — ขั้นที่หายไป
- T+10 ทวง **เฉพาะเมื่อเจ้าของยังไม่พูดอะไรเลยหลังถึงเวลา** (นับ `KikiChat` ของจริง ไม่เดา · นับไม่ได้ = เงียบไว้)

เทสจริง 5 เคสผ่านหมด (ยิง cron ของจริง ใช้ chatId แยกกันข้อความหลุดเข้าแชทเจ้าของ):
45 นาที → ขั้น 1 ชม. · 8 นาที → ขั้น T-10 ไม่ใช่ขั้น 1 ชม. · ถึงเวลา → T-0 · ยิงซ้ำรอบสอง = เงียบ ·
เลยเวลา 12 นาที + เงียบ → ทวง · เลยเวลาแต่เจ้าของเพิ่งพูด → **ไม่ทวง**

**B2. reply แล้วไม่ต่อเรื่อง — บริบทหล่นหายตอนโยนเข้างานเบื้องหลัง**
`handlers/jobs.ts` ส่ง `queueHermesJob(chatId, text)` — **แค่ `text` ไม่มี `replyText`**
เคสจริง: reply ผลงานเก่า ("หาตัวต่อจอ 2 จอ") แล้วพิมพ์ "ไปหาใหม่หน่อย งบ 500"
ตัวรับงานได้ไปแค่ประโยคหลัง → ถามกลับว่า "หาอะไร" → งานจบเปล่า
แล้วพอ reply ถามซ้ำ "ไหนข้อมูลครับ" → เข้า `job_status` → **ตอบ "ไม่มีงานค้าง ว่างสนิท"**

- `planJob()` รับ `replyText` แล้วคืน `brief` = โจทย์ที่ "คนไม่เคยเห็นแชทนี้" อ่านแล้วทำได้ทันที
  (ยังคงกฎเดิม: ห้ามให้ข้อมูลส่วนตัวหลุดไปกับโจทย์ · `needsPrivate` ยังกันงานส่วนตัวไม่ให้ฝากออก)
- `deliveredText` + `parentId` ใน `KikiHermesJob` + `findJobByDelivered()` — จับคู่ด้วยการซ้อนทับของข้อความ
  ไม่ใช่ regex รูปแบบ (ข้อความส่งออกผ่านชั้นเรียบเรียงแล้ว คำพูดเปลี่ยนทุกครั้ง จะฟิกรูปแบบไม่ได้)
- `job_status` ที่ reply ผลงานเก่า = ตอบจากผลนั้น **ห้ามพูดว่า "ไม่มีงานค้าง"**

เทสจริงผ่าน: โจทย์ที่ส่งออกกลายเป็น *"หาตัวต่อจอ 2 จอ (USB-C/HDMI hub…) ราคาไม่เกิน 500 บาท ที่ขายในไทย
เสนอมาหลายตัวเลือกพร้อมชื่อรุ่น ราคา และลิงก์ร้าน"* + `parent` ผูกกลับงานเดิมถูกตัว ·
ถาม "งานถึงไหนแล้ว"/"เสร็จยัง" ขณะ reply ผลเก่า → ตอบจากผลจริง ไม่พูดว่าว่าง

**B3. ผลงาน Hermes ลงท้าย "ค่ะ" — ผลดิบออกแชทตรง ๆ ไม่ผ่านชั้นเรียบเรียง**
`cron/route.ts` ต่อสตริง `` `งานที่ฝากไว้เสร็จแล้วครับ...\n\n${d.body}` `` — ตัวรับงานเป็นคนละตัวกับ Vex
→ ครอบด้วย `askKiki` ก่อนส่งเสมอ โดยบังคับว่า **ตัวเลข ราคา ชื่อรุ่น ลิงก์ ต้องยกมาครบเป๊ะ ห้ามแต่งเพิ่ม**
และถ้าผลที่ได้เป็นแค่คำถามกลับ ให้บอกเจ้าของตรง ๆ ว่ารอบนี้ยังไม่ได้ของ

เทสจริง 2 เคสผ่าน: ผลดิบที่เป็นคำถามลงท้าย "ค่ะ" → ออกมาเป็น *"รอบนี้ยังไม่ได้ของครับ ผู้ช่วยเบื้องหลังส่งกลับมาเป็นคำถามล้วน ๆ…"* ·
ผลดิบที่มีของจริง → ตัวเลข/ลิงก์ครบทั้ง 7 จุด ไม่มีหายสักตัว ไม่มีคำว่า "ค่ะ"

**B4. Hermes ถามกลับแล้วจบงาน — เติมคำสั่งใน `HERMES_GUARD`**
บอกตรง ๆ ว่ามันคุยกับเจ้าของไม่ได้ คำถามที่เขียนจะไม่มีใครตอบ · กำกวม = ตีความแล้วลงมือเลย
**ผล A/B ไม่ชี้ขาด:** ยิงโจทย์กำกวมตัวเดียวกันเข้าทั้ง guard เก่าและใหม่ — **ได้ของจริงทั้งคู่**
แปลว่าอาการ "ถามกลับแล้วจบ" ไม่เสถียร ไม่ได้เกิดจาก guard อย่างเดียว
**ตัวที่แก้ปัญหานี้จริงคือ B2** (โจทย์ที่ส่งออกไม่กำกวมตั้งแต่ต้น) + **B3** (ถ้ายังถามกลับ เจ้าของก็ยังรู้เรื่อง)
guard ใหม่เป็นชั้นกันเพิ่ม ไม่ใช่ตัวหลัก · หมายเหตุ: Hermes ยังลงท้าย "ค่ะ" อยู่ที่ต้นทาง แต่ B3 กรองออกให้แล้ว

**5 ส.ค. 2026 (บ่าย) — ซ่อมของที่พังเงียบ + เปลี่ยนคำเรียกเจ้าของเป็น "โด้"**

*ทั้งหมดนี้ไม่มีอันไหนที่ระบบร้องบอกเอง ต้องไปไล่อ่านล็อกกับวัดเวลาจริงถึงเจอ*

**1. watchdog ตายเงียบ ๆ มา 2 ชั่วโมง — ตัวที่คอยกู้ระบบ กู้ตัวเองไม่ได้**
`ReferenceError: Cannot access 'SESSION_FILE' before initialization` · พัง 21 รอบติด · `LastExitStatus 256`
`const SESSION_FILE` ถูกประกาศ **ใต้** ลูป top-level ที่เรียกใช้มัน — ฟังก์ชัน hoist ได้ แต่ `const` ติด TDZ
เข้ามาจากคอมมิต `4d27c5e` (11:13) แล้วเซสชันจบตอน 11:18 เลยไม่ทันเห็น
→ ย้ายขึ้นไปเหนือลูป · **กติกาที่ได้: สคริปต์ที่รันด้วย StartInterval ต้อง `node --check` + รันจริงดูโค้ดออกทุกครั้ง ไม่งั้นตายแล้วไม่มีใครรู้**

**2. watchdog กล่าวหาผิดตัว แล้วสั่งงานเจ้าของฟรี ๆ**
เว็บล่มเมื่อเช้า → ทุก service พ่น `fetch failed` พร้อมกัน → `SOFTERR` จับได้ครบ 3 รอบ →
สรุปว่า **"เซสชัน REFUND หมดอายุ"** แล้วยิงเข้า Discord ทั้งเสียงและข้อความให้ไปรัน `thunder:auth`
ความจริง: เซสชันไม่ได้หมด (poll รอบ 11:59 สำเร็จเอง · ไฟล์เซสชันยังเป็นของ 3 ส.ค.)
→ แยก `AUTHERR` (หลักฐานตรง เชื่อได้เสมอ) ออกจาก `SOFTERR` (แค่ต่อไม่ติด เชื่อได้ต่อเมื่อเว็บเรายังดี)
**นี่คือกับดักข้อ "web ล่ม = ทุกอย่างพัง" ที่จดไว้แล้ว แต่ watchdog ไม่ได้เอากฎนั้นมาใช้กับตัวเอง**

**3. การ์ดมอนิเตอร์ยังแช่แข็งเว็บอยู่ — เมื่อวานแก้ไปแค่ครึ่งเดียว**
วัดจริงหลังเว็บขึ้น 83 นาที: `/api/kiki/monitor` เร็ว 54 ครั้ง แต่**ช้า 23 ครั้ง 4-32 วินาที**
รากปัญหา 3 ชั้นซ้อนกัน:
- `readUsage()` เป็น `readFileSync` ล้วน → ไม่ได้แค่ช้า แต่ **บล็อก event loop ทั้งเว็บ**
  (หลักฐาน: `POST /api/kiki/ingest 401 in 10.8s` ทั้งที่ตัวงานเองใช้ 184 ms — ที่เหลือคือรอคิว)
- อ่านไฟล์ **3 รอบ** (session/week/today แยกกันคนละรอบ) ทั้งที่ข้อมูลชุดเดียวกัน
- มีคนอ่านของหนักนี้ **2 ทางแยกกัน** — การ์ด (แคช 5 นาที) กับตัวเช็คใกล้เต็ม (`readUsage()` ตรง ๆ ไม่ผ่านแคชเลย)

→ ยกเครื่อง `usage.ts`: อ่านรอบเดียว + **แคชรายไฟล์ด้วย mtime+size** (log ที่เขียนจบแล้วไม่มีวันเปลี่ยน
รอบต่อไปจึงอ่านใหม่เฉพาะไฟล์ที่กำลังถูกเขียนจริง ๆ) + async I/O ทั้งเส้น
+ `readUsageCached()` เป็นทางเข้าเดียว มี singleflight กันยิงพร้อมกันตอนเว็บเพิ่งขึ้น

เจอเพิ่มตอนวัดผลจริง: แคชอยู่ในหน่วยความจำ **หายทุกครั้งที่เว็บรีสตาร์ท**
และเว็บรีสตาร์ทบ่อยมาก (watchdog กู้ให้ · dev reload) — ตอนบูตคือช่วงเครื่องยุ่งที่สุดพอดี
วัดได้ 30-41 วินาที ทั้งที่เครื่องว่างใช้แค่ 6 วิ → เก็บแคชรายไฟล์ลงดิสก์ด้วย (`.usage-cache.json` 0.8 MB)

| | เดิม | ใหม่ |
|---|---|---|
| สแกนซ้ำ (โปรเซสเดิม) | 7,911 ms | **74 ms** |
| **รอบแรกหลังรีสตาร์ทเว็บ** | **30-41 วินาที** | **3.6 วินาที** (0.6 วิเป็นเวลาคอมไพล์) |
| เว็บตอบคนอื่นระหว่างสแกน | ค้างยาว 10.6 วิ | **0.06-0.38 วิ** |

**กับดักตอนวัดผล (เกือบหลงคิดว่าตรรกะพัง):** รอบแรกเทียบแล้วตัวเลขไม่ตรง 8 จุด —
แต่ตัวใหม่ได้เลข *มากกว่า* เสมอ เพราะเซสชัน Claude ที่กำลังทำงานอยู่เขียน log สดตลอดเวลา
ใครวัดทีหลังก็ได้เลขเยอะกว่า · พิสูจน์โดยสลับลำดับการวัด → ส่วนต่างเป็น 0 ทุกค่า
**บทเรียน: เทียบตัวเลขบนข้อมูลที่ยังขยับอยู่ ต้องวัดติด ๆ กันแล้วสลับลำดับดูก่อนสรุปว่าพัง**

**พิสูจน์ว่าตัวเลขไม่เพี้ยน:** เขียนตัวเทียบที่รันตรรกะเก่ากับใหม่พร้อมกันบนข้อมูลจริง —
**ตรงกันทั้ง 30 ค่า** (2 provider × 3 หน้าต่าง × 5 ตัวเลข)
เลือกเก็บ event ทุกตัวแทนการปัดเป็นถังเวลา เพราะวัดแล้วมีแค่ 21,110 event (<1 MB) จะได้ไม่มีขอบหน้าต่างคลาดเคลื่อน

**4. ท่อ Discord ผูก listener ทับซ้อน**
`joinVoiceChannel()` คืน connection **ตัวเดิม** ของ guild ที่มีอยู่แล้ว → ต่อกลับทีนึงผูก listener เพิ่มอีกชุด
ขึ้น `MaxListenersExceededWarning` ที่ 11 ตัว แล้ว event ถูกพิมพ์ซ้ำเป็นสิบรอบจนล็อกอ่านไม่รู้เรื่อง
→ ล้างของเก่าก่อนผูกใหม่เสมอ (แบบเดียวกับที่ `attachReceiver` ทำกับ `speaking` อยู่แล้ว)

**5. เรียกเจ้าของว่า "โด้" — จ่ายหนี้ที่ค้างมาตั้งแต่เฟส 0.5**
- persona เปลี่ยนเป็น `เรียกเจ้าของว่า "โด้" คำเดียวเท่านั้น` + ห้าม "พี่"/"พี่โด้"/"คุณโด้" + ห้ามลงท้าย "ครับพี่"
- กวาดข้อความฮาร์ดโค้ด 29 จุดใน handlers/cron/persona เทรนเนอร์
- **ที่จงใจไม่แตะ** (ไม่ใช่การเรียกเจ้าของ): ตัวจับเสียงคนอื่นคุยโทรศัพท์ใน `kiki-listen.ts` ·
  regex จำแนกหมวด "คนรอบตัว" ใน `memory.ts` · ชื่อคนอื่นที่ยกเป็นตัวอย่าง ("พี่ภูมิ")
- เทสจริงผ่าน ingest: ทักทาย → *"ดีครับโด้ ..."* · ถามตรง ๆ → *"โด้ ครับ คำเดียว ไม่มีพี่ ไม่มีคุณนำหน้า"*
- ลบข้อมูลเทสออกครบทั้ง `KikiChat` และเวกเตอร์ (กวาดเวกเตอร์กำพร้าเจอ 27 ตัว ลบทิ้งหมด)

**4 ส.ค. 2026 — เฟส 1 เสร็จ**
- `kiki-router.ts` ตัวจัดเส้นทางด้วย Gemini flash (~1.4 วิ/ครั้ง, Claude CLI สำรอง) แทน regex 25 จุดใน ingest — เทส 15 เคสจริงผ่าน 15/15 รวมเคสที่เคยพัง ("คิดชื่อกลุ่มให้หน่อย อยากได้..." เคยชน WISH_RE)
- `INTENT_CATALOG` เป็นแหล่งเดียวของทั้ง router และรายการความสามารถใน persona — เพิ่มความสามารถใหม่แก้ที่เดียว ไม่หลุด sync
- `kiki-tasks.ts` + model `KikiTask` — จด/ลิสต์/ปิด/ทวง + งานมีเงื่อนไข (triggerText) เตือนตอนเจ้าของพูดถึง
- `kiki-memory.ts` + ตาราง `kiki_recall` (sqlite-vec + bge-m3) — index แชท 397 ข้อความย้อนหลังแล้ว, recall ~0.4 วิ, สรุปรายวันเก็บใน `KikiMemory` (ไม่เขียนลง Obsidian ตามกติกาเก็บเฉพาะที่สั่ง)
- หน้าต่างบทสนทนา 18×500 → 40×1200 ตัวอักษร (เพดานรวม 20k)
- `kiki-format.ts` + เลิกซอยบับเบิลรายบรรทัด (สูงสุด 3 ก้อน) + บรีฟเช้าเขียนใหม่เป็นหัวข้อ/บรรทัดละรายการ
- heartbeat ทุก 3 นาทีของงาน Hermes/พัฒนา + ปุ่ม "หยุดงานนี้" · ตัวรันงานพัฒนาเปลี่ยนเป็น stream-json = รายงานได้ว่ากำลังแก้ไฟล์ไหน

**4 ส.ค. 2026 — เฟส 2 + เฟส 3 เสร็จ**
- `kiki-read.ts` ท่ออ่านเดียว: YouTube → ดูคลิปจริง · X/IG/FB/TikTok/LinkedIn → เปิดใน Chrome ที่ล็อกอิน · ที่เหลือ fetch แล้วถอยไปเบราว์เซอร์ถ้าเนื้อบาง · อ่านไม่ได้ = บอกตรง ๆ ห้ามเดา
- `summarizeLong` สรุปเอกสารยาวแบบไล่เป็นก้อน — เลิกตัดทิ้งที่ 24,000 ตัวอักษร
- ไฟล์ที่ส่งมา = อ่านลึกแล้วตอบ **แต่ไม่เก็บเข้าคลัง** ถ้าไม่ได้สั่ง (กติกาเจ้าของ)
- `kiki-chrome.ts` สะพาน CDP ไป Chrome ตัวจริง: `readUrl` (ข้อความ + แคปภาพ), `draftReply` (พิมพ์ค้างไว้ ไม่กดส่ง), `sendDraft` (กดส่งหลังยืนยัน), `discardDraft`, `socialLoginStatus`
  - Chrome 136+ ห้าม remote debugging บนโปรไฟล์ Default → ใช้โปรไฟล์แยก `~/.vex-chrome` เปิดด้วย `npm run kiki:chrome`
  - เทสแล้ว: ต่อ CDP ได้ · อ่าน x.com ได้จริง 1,923 ตัวอักษร + แคปภาพ (ยังไม่ล็อกอิน = ยังอ่านโพสต์ส่วนตัวไม่ได้)
- `KikiMedia` + `kiki-media.ts`: เก็บรูป/วิดีโอ **เมื่อสั่ง** → วิชันบรรยายเนื้อในรูป → ค้นคืนด้วยความหมาย (เทสจริงผ่าน: เก็บ → ขอคืน → ได้ไฟล์ถูกตัว) · บอทรับวิดีโอ/GIF แล้ว
- ตอบรายการเงินรอระบุทีเดียวหลายตัว (`classifyPendingBatch`) + ขอลิสต์เต็มได้ทุกเมื่อ
- คุม Mac: ระบบตรวจเองว่ามีไฟล์ภาพจริงไหม ถ้า AI เคลมว่าแคปแล้วแต่ไม่มีไฟล์ = ต่อท้ายคำเตือนจากระบบ

**4 ส.ค. 2026 — เฟส 4 เสร็จ**
- `kiki-advice.ts` โหมดที่ปรึกษาการเงิน: เจ้าของเล่ารายละเอียดยาว ๆ → วิเคราะห์จากตัวเลขจริงในระบบ (งบ/หนี้/บิล/pace) → จำข้อเท็จจริงการเงินถาวร + ลงงานที่ต้องทำเข้ากระดานอัตโนมัติ + เก็บ "แผน" ไว้เทียบทีหลัง
- รายงานอาทิตย์เย็นเพิ่มหัวข้อ "รีวิวเทียบแผน": ทำตามแผนได้กี่ข้อ ค้างอะไร ต้องแก้อะไรสัปดาห์หน้า

**สิ่งที่เจ้าของต้องทำเอง (ครั้งเดียว)**
1. ล็อกอิน X / Facebook / Instagram ในหน้าต่าง Chrome ของ Vex (เปิดด้วย `npm run kiki:chrome` — ตอนนี้เปิดค้างไว้แล้ว โปรไฟล์ `~/.vex-chrome`) แล้วเปิดทิ้งไว้
2. ถ้าอยากให้เปิดเองตอนล็อกอินเครื่อง สั่งได้ เดี๋ยวทำ LaunchAgent ให้ (ตอนนี้ Vex เปิด Chrome เองอัตโนมัติเมื่อจำเป็นอยู่แล้ว)

**4 ส.ค. 2026 (ค่ำ) — เทสโซเชียลของจริงหลังเจ้าของล็อกอินครบ**
- ล็อกอินครบทั้ง X / Facebook / Instagram ในโปรไฟล์ `~/.vex-chrome` แล้ว (เช็คผ่าน `socialLoginStatus`)
- อ่านโพสต์ X รายตัวได้จริง 4,500 ตัวอักษร (รวมคอมเมนต์ใต้โพสต์) — เคสที่เคยพังสนิท "ลิงก์นี้ระบบดึงเนื้อหาไม่ได้ ขึ้นหน้าเปล่า"
- ทดสอบผ่าน Telegram จริง: ส่งลิงก์โพสต์ + "ดูอันนี้ให้หน่อยมันดีมั้ย" → ได้แคปหน้าจอโพสต์ + วิเคราะห์อิงเนื้อหาจริง (จับได้ว่าเป็นโพสต์ล่า engagement, คัดของที่ใช้ได้จริงมา 3 ข้อโดยอ้างเป้าหมาย Dev 3 เดือนของเจ้าของจากความจำ)
- ร่างข้อความบน X: พิมพ์ค้างในหน้าจริง + แคปยืนยัน + แท็บอยู่รอจนกดส่ง (ยืนยันด้วยภาพแล้ว) — ยังไม่เคยกดส่งจริงสักครั้ง รอเจ้าของสั่ง
- LaunchAgent `com.changoh.vexchrome` เปิด Chrome ให้อัตโนมัติตอนล็อกอินเครื่อง (RunAtLoad, ไม่ KeepAlive)

**4 ส.ค. 2026 (ดึก) — กวาดข้อความสำเร็จรูปทั้งระบบ + เจอบั๊กตัวตัดคำไทย**
- สแกนทางตอบทั้งหมด 67 จุด → ครอบด้วย `vexLine()` 53 จุด (ใจความ/ตัวเลข/คำสั่งคงเดิม แต่ถ้อยคำใหม่ทุกครั้ง ~1.5 วิ ล่มแล้วส่งข้อความเดิม)
- อีก 15 จุดคงไว้โดยตั้งใจ ทำเครื่องหมาย `// canned-ok:` พร้อมเหตุผล (โควตข้อความที่จะส่งจริง · ลิสต์ข้อมูลดิบ · ตัวดักพังชั้นสุดท้าย)
- `npm run kiki:check` = ตัวตรวจว่ามีข้อความสำเร็จรูปหลุดกลับมาไหม (ต้องผ่านก่อน commit) + `docs/vex-code-rules.md` + ฝังกติกาใน DEV_RULES ของตัวพัฒนาตัวเอง
- **บั๊กที่เจอระหว่างกวาด: ตัวตัดคำไทยพังทั้งระบบ** — `[^\p{L}\p{N}\s]` ลบสระ/วรรณยุกต์ (Unicode Mark) ทิ้ง ทำให้ "ขอรูปตอนไปเที่ยวทะเล..." กลายเป็นเศษ ["ขอร","ปตอนไปเท","อย"] แล้ว "อย" ไปแมตช์ข้อความอื่นมั่ว
  - แก้เป็น `\p{M}` ครบทุกจุด (kiki.ts / kiki-tasks.ts / kiki-media.ts) + ขั้นต่ำ 3 ตัวอักษร + ต้องได้คะแนน ≥3 ถึงนับว่าเกี่ยว
  - กระทบตั้งแต่ค้นโน้ตส่วนตัว ค้นรูป หางานในกระดาน ไปจนถึงเงื่อนไขเตือน — ทั้งหมดเคยจับมั่วได้

**5 ส.ค. 2026 (ค่ำ) — ก้อน A: เซสชันดูแลตัวเอง + ตัวรันแบบโต้ตอบ**

โจทย์เจ้าของ: *"ทุกเซสชันมันหมดอายุ ผมอยากให้มันรันเองได้ / เซสชันไหนหมดให้แจ้งไปที่ในแชท
แล้วผมจะบอกให้รัน ให้คุณรันให้ผมเลย ส่วนถ้ามันมีอะไรให้กรอกก็แคปหรือถามผมได้เลย"*

**ช่องว่างที่ของน้องวานไม่มี**
`waan-ops.ts` มีครึ่งเดียว: แจ้ง → ตอบ "รันเลย" → รันให้ → รายงาน
แต่ `runNpmScript()` **ป้อน stdin ไม่ได้** ใช้ได้เฉพาะตัวที่เด้งหน้าต่างเบราว์เซอร์เอง (`thunder:auth`)
`kiki:tg-auth` ต้องพิมพ์เบอร์ + OTP ลงเทอร์มินัล — ต้องมีคนถือ stdin ไว้จนจบ

**ที่ทำ**

| ส่วน | ไฟล์ |
|---|---|
| ตัวขับที่ถือ stdin ไว้ (คนละโปรเซสกับเว็บ คุยกันผ่านไฟล์) | `scripts/vex-auth-run.mjs` |
| ทะเบียนเซสชัน + ตรวจสุขภาพ + ตรวจหลักฐานผล | `src/lib/vex-ops.ts` |
| ถาม OTP ในแชท + ปุ่มจากใบแจ้ง | `handlers/session.ts` |
| แจ้งเองเมื่อเซสชันหมด + ตามถามระหว่างล็อกอิน | `cron/route.ts` (G1.5 · G1.6) |

**ทำไมตัวขับต้องเป็นคนละโปรเซสกับเว็บ:** เว็บรีสตาร์ทบ่อยมาก (watchdog กู้ · dev reload)
ถ้าถือ stdin ไว้ในเว็บ การล็อกอินจะตายกลางคันทุกครั้งที่เว็บเกิดใหม่ → แยกออกไป detached
คุยกันผ่านไฟล์ `.vex-auth/<id>.{log,in,status}` เว็บรีสตาร์ทกี่รอบก็ไม่กระทบ

**ตรวจสุขภาพจากหลักฐานของตัวเอง ไม่ใช่จากอาการ**
- ยกบทเรียนของวานมาใช้: เว็บล่ม = ทุกอย่างพ่น error พร้อมกัน **ห้ามสรุปว่าเซสชันหมด**
  (วานเคยกล่าวหาผิดตัวแล้วสั่งงานเจ้าของฟรี ๆ เมื่อ 5 ส.ค. เช้า)
- **โปรเซสยังอยู่ ≠ ยังทำงาน** — `vex-eyes` ตายเงียบ 4.5 ชม. โดยโปรเซสยังรันอยู่
  → เกณฑ์คือ log ที่ต้อง "ขยับ" จริง (`heartbeatMin`) ไม่ใช่ `pgrep`
- เคลมว่าล็อกอินสำเร็จได้ต่อเมื่อ **ไฟล์เซสชันถูกเขียนใหม่หลังงานเริ่ม** (กติกาข้อ 3)

**ตรวจจับ "โปรเซสรออะไรอยู่"** — ดูบรรทัดท้ายที่ยังไม่ขึ้นบรรทัดใหม่และลงท้ายด้วย `:` / `?`
เป็นการอ่านผลลัพธ์ของโปรแกรม ไม่ใช่เดาเจตนาคน (กติกาข้อ 1 ยอมให้)
ส่วน "ข้อความที่เจ้าของพิมพ์มาใช่คำตอบของคำถามนี้ไหม" **ให้สมองตัดสิน** — "60539" กับ
"เดี๋ยวก่อน" ต้องแยกออกจากกัน และแปลง `0831245989` → `+66831245989` ให้เอง

**เทสจริงผ่าน 8/8**
- ถามสถานะ → ลิสต์ 🟢/🔴 พร้อมหลักฐานรายตัว
- ย้อน mtime ของ log ไป 3 ชม. → จับได้ว่า "เงียบมา 180 นาที เกินเพดาน 90"
- cron แจ้งเองพร้อมปุ่ม "รันเลย / ไว้ก่อน" · แจ้งรอบสองใน 2 ชม. = เงียบ (ไม่สแปม)
- **สั่งล็อกอินทั้งที่เซสชันยังดี → ปฏิเสธ** พร้อมอธิบายว่ามันฟื้นเองแล้ว
- ตัวขับ spawn จริง → เขียนครบ 3 ไฟล์ → จับคำถามแรก `"เบอร์โทร (เช่น +66812345678):"` ได้ถูกตัว
- แคปการ์ดหน้าจอเทอร์มินัลออกมาเป็นภาพจริง 56 KB อ่านได้
- (หยุดก่อนกรอกเบอร์ เพื่อไม่ให้ Telegram ส่ง OTP จริงมารบกวนเจ้าของ)

**พิสูจน์ท่านี้ด้วยมือมาก่อนแล้ว** — 5 ส.ค. 17:34 ล็อกอิน Telegram userbot สำเร็จ (T3 @T3limit)
โดยเจ้าของแค่บอกเบอร์กับ OTP ในแชท ไม่ต้องเปิดเทอร์มินัลเลย · ตาของ Vex กลับมาทำงานแล้ว

**5 ส.ค. 2026 (ค่ำ) — ก้อน C: เทสของจริง (C1 เสร็จ · C2 เจอบั๊กใหญ่ ยังเทสไม่ครบ)**

**C1 อ่านคลิป YouTube — ผ่าน แต่เจอว่าเส้นทางผิดมาตลอด**
ท่อ `summarizeYoutube` **ดูคลิปจริง** พิสูจน์แล้ว: บรรยายได้ถึงเสื้อแจ็กเก็ตแดง-เทา ช้าง 2 ตัว
โซ่ที่ขาช้าง ฟางแขวน หินเทียมด้านซ้าย — ไม่มีในบทความไหนบนเว็บ · 3Blue1Brown ให้ timestamp รายช่วงได้

**บั๊กที่เจอ: ลิงก์ YouTube ถูกส่งไป Hermes ซึ่งดูคลิปไม่ได้**
Hermes ขูดหน้าเว็บมาเล่า — คลิปดังยังได้คำตอบถูกเพราะมีบทความให้ขูด
**แต่คลิปที่ไม่มีใครเขียนถึงจะพังเงียบ** และเจ้าของไม่มีทางรู้ว่าคำตอบไม่ได้มาจากคลิป
→ กันไว้ 2 จุด: `autoBackgroundHandler` และ `hermesHandler` เจอ YouTube = ปล่อยให้ท่อ Vex รับ
ต่อให้เจ้าของสั่ง "ฝาก Hermes" ตรง ๆ ก็ไม่ส่ง — ได้ของปลอมแย่กว่าช้า
เทสซ้ำหลังแก้: ตอบสดในแชท 64 วิ / 49 วิ ได้เนื้อจากคลิปจริงทั้งคู่

**C2 พิมพ์คำสั่งใน Warp — คำสั่งรันจริง ไม่กวนเซสชัน Claude แต่รายงานผิด**

*แก้ตามที่เจ้าของเตือน:* `runInWarp` เดิมพิมพ์ลงแท็บที่โฟกัสอยู่ ซึ่งมักเป็นแท็บที่มีเซสชัน
Claude รันค้าง → คำสั่งกลายเป็นข้อความสั่ง Claude แทนคำสั่งเชลล์
ตอนนี้ **เปิดแท็บใหม่เสมอ** ถ้าไม่ได้ระบุแท็บ · เปิดแท็บใหม่ไม่ได้ = ไม่พิมพ์เลย (ไม่เสี่ยง)

เทสจริง (เครื่องมีเซสชัน Claude 19 ตัวรันอยู่): คำสั่งเขียนไฟล์ token → **ไฟล์โผล่พร้อมค่าถูกต้อง**
= ลงเชลล์จริงและรันจริง · เซสชัน Claude ทั้ง 19 ตัวเหมือนเดิมเป๊ะ ไม่มีตัวไหนตาย/เพิ่ม

**บั๊กใหญ่ที่เจอระหว่างทาง: ภาพหลักฐานเป็นของคนละ Space**
`screencapture` เก็บได้แค่ Space ที่แสดงอยู่บนจอหลัก · เจ้าของจัด Warp ไว้อีกเดสก์ท็อป
→ ภาพ "หลักฐาน" ทุกใบเป็นภาพ Chrome ทั้งที่ frontApp รายงานว่า `stable` (Warp) ถูกต้อง
→ ตัวตรวจด้วยวิชันมองไม่เห็นคำสั่ง เลยฟันธงว่า **"ส่งไม่สำเร็จ"** ทั้งที่สำเร็จ

นี่คือกลับด้านของกติกาข้อ 3 — ไม่ได้เคลมเกินจริง แต่ **ปฏิเสธความจริง** และแย่กว่านั้นคือ
ภาพที่แนบไปดู "เหมือนหลักฐานจริง" ทั้งที่เป็นคนละแอป
→ `seenOnScreen()` ถามวิชันเพิ่มว่า "ในภาพมีแอปที่สั่งไหม" ไม่มี = คืน `null` (ตรวจไม่ได้)
ไม่ใช่ `false` — **"มองไม่เห็น" คนละเรื่องกับ "เห็นแล้วว่าไม่มี"**

**ยังค้าง (ทำต่อรอบหน้า):** เทส C2 ซ้ำหลังแก้ตัวตรวจ · หาวิธีแคปหน้าต่างข้าม Space ให้ได้จริง
(ตอนนี้แค่ไม่โกหก แต่ยังส่งภาพที่ไม่ใช่หน้าต่างเป้าหมายอยู่) · ก้อน D ยังไม่เริ่ม

**5 ส.ค. 2026 (ค่ำ) — C2 ปิดจบ: แคปหน้าต่างข้าม Space ได้แล้ว**

**ปัญหา:** `screencapture` เก็บได้แค่ Space ที่แสดงอยู่บนจอหลัก เจ้าของจัด Warp ไว้อีกเดสก์ท็อป
→ ภาพ "หลักฐาน" ทุกใบเป็นของ Chrome ทั้งที่คำสั่งรันสำเร็จ → ตัวตรวจฟันธงผิดว่า "ส่งไม่สำเร็จ"

**ทางแก้:** `CGWindowListCopyWindowInfo(kCGWindowListOptionAll)` เห็นหน้าต่างทุก Space
ได้ window id แล้วแคปด้วย `screencapture -l <id>` ซึ่งข้าม Space ได้

**ทำไมต้องเขียนเป็นภาษา C** (ลองมาแล้วทุกทาง)
- JXA เรียก `CGWindowListCopyWindowInfo` ไม่ได้ — ไม่มีใน bridge (`$.CGWindowList...` = undefined)
- pyobjc ไม่ได้ติดตั้งในเครื่อง (`No module named 'Quartz'`)
- swift toolchain ในเครื่องไม่แมตช์กับ SDK (`redefinition of module 'SwiftBridging'`)
- clang + CoreGraphics ที่เป็น C API ล้วน = ทางที่เหลือและง่ายสุด · คอมไพล์เองครั้งแรกที่ใช้

| ส่วน | ไฟล์ |
|---|---|
| ลิสต์หน้าต่างทุก Space | `scripts/mac/winlist.c` |
| คอมไพล์อัตโนมัติ + หา/แคปหน้าต่าง | `src/lib/kiki-window.ts` |
| `snap(label, app)` แคปหน้าต่างก่อน ถอยไปเต็มจอถ้าหาไม่เจอ | `kiki-gui.ts` |

**เทสจริงหลังแก้ (บนเครื่องที่มีเซสชัน Claude 19 ตัวรันอยู่)**
- เดิม: *"ส่งไม่สำเร็จครับ ⚠️ ตอนกดส่งเหมือนข้อความยังไม่เข้าช่องพิมพ์"* (ทั้งที่สำเร็จ)
- ใหม่: *"ส่งขึ้นในห้องเรียบร้อยครับ (ยืนยันจากภาพหน้าจอที่แนบมา 5 รูป)"*
- ภาพที่แนบเป็น **หน้าต่าง Warp จริง** เห็นคำสั่งอยู่ใน **แท็บใหม่** ไม่ใช่แท็บที่มีเซสชันรันอยู่
- ไฟล์ token ถูกเขียนถูกต้อง · เซสชัน Claude ทั้ง 19 ตัวครบเหมือนเดิม

**เกร็ดที่เจอ:** เซสชัน Claude ที่กำลังพัฒนา Vex อยู่นี้ รันอยู่ใน Warp แท็บ "พัฒนาเลขา Vex 1" เอง
— แท็บที่ห้ามกวนคือแท็บของตัวเองพอดี ยิ่งพิสูจน์ว่ากฎ "เปิดแท็บใหม่เสมอ" จำเป็นจริง

**ผลข้างเคียงจากการเทส:** มีแท็บ Warp เปล่าเพิ่มมา 2 แท็บ (จากเทส 2 รอบ) ปิดทิ้งได้เลย

**5 ส.ค. 2026 (ดึก) — ก้อน D: ยกเครื่องความฉลาด**

โจทย์เจ้าของ: *"โดยรวมแล้วยังไม่สมบูรณ์ ไม่ฉลาด ผมต้องการพัฒนาให้มันมีความรู้มากกว่านี้
และสามารถวิเคราะห์ตัดสินใจได้มากกว่านี้"*

**รากปัญหาที่วัดได้จริง**
router เลือก **เจตนาเดียว** → ตัวจัดการเดียว → ตอบ → จบ · `chat.ts` ประกอบ prompt ก้อนใหญ่
แล้วเรียกสมองรอบเดียว **ไม่มีจังหวะ "เห็นผลแล้วคิดต่อ"** → งานหลายขั้นทำไม่ได้เลย

**D1 — ลูปใช้เครื่องมือ (`kiki-agent.ts`)**
วางแผน → เรียกเครื่องมือ → เห็นผลจริง → คิดต่อ → ตอบ (สูงสุด 5 รอบ · เพดานเวลา 110 วิ)
เครื่องมือ 8 ตัว **อ่านอย่างเดียวทั้งหมด**: ค้นความจำ · การเงินจริง · นัด · งานค้าง · อ่านลิงก์ ·
ค้นเว็บสด · ข้อเท็จจริงเจ้าของ · โปรไฟล์

*ทำไมแยกสองชั้น (หาข้อมูล → ให้สมองหลักเรียบเรียง) แทนให้ลูปตอบเอง*
เสียง/บุคลิกของ Vex อยู่กับสมองหลัก ถ้าให้ลูปตอบตรง ๆ เสียงเปลี่ยนทันทีและกติกาข้อ 2 พัง
ชั้นนี้ทำหน้าที่เดียวคือ **"รู้ให้มากขึ้นก่อนพูด"**

*ทำไมเครื่องมืออ่านอย่างเดียว* — ลูปที่ตัดสินใจเองได้ + สั่งการได้จริง = พลาดทีเดียวแก้ไม่ได้
ทางเขียนยังเดินผ่านตัวจัดการเดิมที่มีขั้นยืนยันครบเหมือนเดิมทุกประการ

**บั๊กที่เจอตอนเทส และเป็นรากของ "ตอบไม่ตรงคำถาม" ที่เจ้าของบ่น**
เทสว่า *"อยากได้คีย์บอร์ด งบสองพัน **หาให้หน่อยว่าตัวไหนดี** แล้ว**ดูให้ด้วยว่าซื้อไหวมั้ย**"*
→ router เลือก `wish` → ได้แค่ *"เข้า wishlist แล้วครับ · ถ้าออมวันละ 100 = 20 วัน"*
**ส่วนที่ขอให้หาของกับดูว่าไหวมั้ย หายไปทั้งดุ้น** — ลูปไม่ได้ถูกเรียกเลยเพราะไม่ได้เข้าทาง chat

→ เพิ่มกติกาข้อ 6 ใน router: **ปลายทางที่จะเลือกทำครบทุกอย่างที่ขอไม่ได้ = ไปทาง chat**
(chat เป็นทางเดียวที่มีเครื่องมือครบและตอบหลายเรื่องจบในครั้งเดียวได้)

*ผลหลังแก้ (คำถามเดียวกันเป๊ะ):* หาของจริงได้ 7 ตัวพร้อมราคา → ฟันธง Logitech Pebble 2 Combo
1,467 บาท พร้อมเหตุผล → เสนอตัวรองถ้าจะประหยัด → **แล้วดึงตัวเลขการเงินจริงมาห้ามซื้อ**
(*"เดือนนี้จ่ายไป 26,041.65 รับเข้ามา 22,879.12 ติดลบ 3,162.53 แล้ว ยังไม่ทันบวกคีย์บอร์ด
และในนั้นเป็นก้อนรอระบุ 23,978.65 = 92% ของทั้งเดือน แปลว่าโด้ยังไม่รู้ด้วยซ้ำว่าเงินหายไปกับอะไร"*)
79 วินาที

**D2 — โปรไฟล์ที่ใช้ตัดสินใจได้ (`kiki-profile.ts`)**
ของเดิมฉีดข้อเท็จจริงดิบทั้งกอง ซึ่งดูของจริงในฐานข้อมูลแล้วเป็นประโยคลอย ๆ ว่า
*"เขาต้องการให้ AI ..."* เกือบหมด (11 ข้อในหมวดเป้าหมาย) — เอาไปเลือกของแทนเขาไม่ได้เลย
→ กลั่นจากข้อเท็จจริง + บทสนทนาจริง 3 สัปดาห์ + ตัวเลขการเงินจริง เหลือเฉพาะสิ่งที่เปลี่ยนคำตอบ:
เป้าหมายตอนนี้ · เพดานเงิน · รสนิยมเวลาเลือกของ · ข้อห้าม · จังหวะชีวิต · ของที่ใช้อยู่
**แยกชัดว่าอันไหนเจ้าของบอกเอง อันไหนเราเดา** — ไม่งั้นข้อสันนิษฐานกลายเป็นข้อเท็จจริง

*กับดักที่เจอ:* วัดจริงแล้วสร้างโปรไฟล์ใช้ **109 วินาที** — ถ้าสร้างสดตอนเจ้าของถาม
เขานั่งรอเกือบสองนาทีโดยไม่รู้ว่ารออะไร → สร้างเบื้องหลังเสมอ รอบแรกยอมไม่มีโปรไฟล์
วัดหลังแก้: คืนค่าใน **10 ms** · เบื้องหลังสร้างเสร็จใน 60 วิ · แคช 12 ชม.
*อีกอัน:* รอบแรกโปรไฟล์ขึ้นต้นว่า "ฉันคือ Vex ... นี่คือโปรไฟล์ของท่าน" — บล็อกนี้ฉีดเข้าหัวสมอง
โดยตรง คำนำจะกลายเป็นเสียงแปลกปลอมและคำว่า "ท่าน" อาจหลุดออกปาก → สั่งห้ามคำนำ/สรรพนาม

**D3 — ฟันธงแทนถามกลับ (persona)**
กำกวม = ตีความแล้วลงมือ พร้อมบอกว่าตีความว่าอะไร · ขาดข้อมูล = ไปหาเองก่อน ·
ต้องเลือก = เลือกให้ + เหตุผล + ทางเลือกรอง · จำเป็นต้องถามจริง = **เสนอคำตอบที่ดีที่สุดก่อน** แล้วค่อยถาม
· เดาความต้องการได้ แต่ **เดาตัวเลข/ข้อเท็จจริงไม่ได้เด็ดขาด**

**เทสจริงผ่าน 3/3** — งานหลายขั้น · อ้างของเก่าลอย ๆ ("เรื่องตัวต่อจอไปถึงไหนแล้ว" → เล่าได้ครบ
พร้อมทักว่าเครื่องเป็น M1 Pro ต่อ 2 จอได้เองอยู่แล้ว ไม่ต้องซื้อ DisplayLink) · คุยเล่นต้องไม่ช้า (9 วิ)

**6 ส.ค. 2026 — 6 อาการที่โด้เจอตอนใช้จริง + ของใหม่ 2 อย่าง**

**1. การล็อกอินค้างอยู่ = ยึดเทิร์นทุกข้อความ (บั๊กที่ผมทำเองเมื่อวาน)**
`authRunHandler` อยู่หัวทะเบียน พอข้อความ "ไม่ใช่คำตอบของ prompt" กลับ `return askOwner(...)`
= **กลืนคำสั่งจริงทิ้ง** · โด้ส่งลิงก์ X มาสั่งให้อ่าน+วิเคราะห์ แล้วสั่งต่อว่าไปดูแท็บ Warp
**ได้ช่องกรอกเบอร์โทรกลับมาทั้งสองครั้ง** → เปลี่ยนเป็นต่อท้ายเป็นหมายเหตุแล้วปล่อยผ่าน
งานเบื้องหลังห้ามมาบังงานที่เจ้าของสั่งจริง

**2. "ไปดู…มีอัปเดทมั้ย" ถูกตีเป็น "พิมพ์"**
ไปพิมพ์ใส่แท็บ Warp ที่มีเซสชันรันค้างอยู่ (แท็บที่โด้ระบุชื่อมาเอง ระบบเลยไม่กันให้)
→ แยกให้ชัดใน catalog: **ถามว่ามีอะไร = อ่าน ไม่ใช่พิมพ์** · `warp_cmd` ต้องมีเนื้อคำสั่งมาด้วยเท่านั้น
→ `gui_switch` เปลี่ยนจาก "สลับให้แล้ว + ภาพ" เป็น **อ่านจอแล้วตอบคำถามที่ถาม**

**3. รายงานดูเฉย ๆ (โด้: "ให้เพิ่มองค์ประกอบ จัดเรียงใหม่ อิโมจิมืออาชีพแนวนี้ 📤 🚀")**
`vexSections` ของเดิม: หัวข้อตัวหนาล้วน ทุกบรรทัดขึ้นต้น "·" เหมือนกันหมด กวาดตาแล้วแยกไม่ออก
→ หัวข้อมีไอคอนนำ · `sub` แยกยอดรวมออกจากชื่อหัวข้อ · ค่าตัวเลขชิดท้ายบรรทัด · `accent` ทำตัวหนา
· เส้นคั่นก่อนบรรทัดสรุป · บรีฟเช้าเรียงใหม่ตาม "ต้องรู้ก่อน": นัด → เงินคงเหลือ → กำหนดจ่าย → งานค้าง → รายละเอียดเมื่อวาน

**4. ถามว่า "คิดว่าตัวเองไม่เก่งอะไร" → ได้การ์ดการเงิน · รอบสองได้ "ช่วยระบุสเปกให้ชัดเจน"**
เจตนา `self_dev` เดิมกินทั้งคำสั่งและคำถาม → แยก `self_reflect` ออกมาเป็นคนละเจตนา
(คำถามถึงตัวเอง ≠ คำสั่งให้ลงมือแก้โค้ด) + เครื่องมือ `read_self_status` อ่านของจริง 3 แหล่ง:
แคตตาล็อกความสามารถ · บรรทัด "ยังค้าง" ในแผนพัฒนา · git log 15 ตัวล่าสุด
และ `selfDevHandler` **เลิกถามกลับเปล่า ๆ** — สกัดสเปกไม่ได้ให้เสนอสเปกที่ทำได้จริง 3 ข้อแทน

*ผลหลังแก้:* ตอบได้ว่าตัวเองพังตรงไหน พร้อมหลักฐาน — เซสชัน Telegram ตายมาแล้ว 4 รอบ
(102/222/271/392 นาที) · แคปข้าม Space ยังส่งภาพผิดหน้าต่าง · 3 คอมมิตล่าสุดเป็นอาการเดิมซ้ำ
แล้วฟันธงว่าควรทำข้อไหนก่อนพร้อมเหตุผล

**5. ลิงก์ + "เก็บเข้าสมอง" มาพร้อมคำสั่งอื่น → อธิบายให้แต่ไม่ได้เก็บ**
เพราะเข้าทาง chat (ถูกแล้ว เพราะขอหลายอย่าง) แต่ไม่มีใครเก็บลิงก์ให้
→ router ส่ง `args.save_link` มาด้วย · chat เก็บให้จริงแล้วบอกว่าเก็บแล้ว (กติกาข้อ 4: เก็บเฉพาะที่สั่ง)

**6. โหมดวิเคราะห์เชิงลึกอัตโนมัติ + ฐานการเงินหลัก 4 อย่าง (โด้สั่งเพิ่ม 6 ส.ค.)**

*โหมดวิเคราะห์เชิงลึก* — ตัวอ่านเจตนาชี้เองว่าโจทย์หนักไหม (`args.deep`) ไม่ใช้ regex จับคำว่า "วิเคราะห์"
เข้าข่าย = วางแผนการเงิน · แผนคุมงบ · แผนอัปสกิล · ตัดสินใจเรื่องใหญ่
โครงบังคับ 7 ขั้น: สรุปสถานการณ์จากตัวเลขจริง → ปัญหาที่แท้จริง → ทางเลือก+ต้นทุน →
**ฟันธงว่าเอาทางไหน** → แผนลงมือมีตัวเลขมีเวลา → ตัวชี้วัด → ความเสี่ยง
กฎ 3 ย่อหน้าไม่ใช้กับโหมดนี้ แต่ตัวเลขทุกตัวต้องมาจากข้อมูลจริง ไม่มีให้ขอเพิ่ม ห้ามสมมติ

*ฐานการเงินหลัก 4 อย่าง* (`kiki-baseline.ts`) — รายรับจริงต่อเดือน · ตัดประจำ · เงินเก็บ · หนี้ค้าง
**ไม่เก็บซ้ำกับของเดิม**: ตัดประจำอ่านจาก `RecurringBill` · หนี้อ่านจาก `Debt` (ทั้งคู่มีอยู่แล้ว)
เก็บเพิ่มแค่ 2 ตัวที่ยังไม่มีที่อยู่ · พิมพ์รวดเดียวได้เลย ระบบแกะเองแล้วลงที่ที่ถูกต้องของแต่ละอย่าง
ขาดข้อไหนต้องบอก — วิเคราะห์บนข้อมูลไม่ครบโดยไม่มีใครรู้คือสิ่งที่อันตรายกว่า

**เทสจริงผ่านหมด** · ข้อควรระวังที่เจอระหว่างเทส: ตัวเลขการเงินที่ใช้เทสเป็นของสมมติ
ถ้าทิ้งไว้ Vex จะเอาไปวิเคราะห์จริง → ลบออกครบทั้ง Setting · RecurringBill · Debt · KikiTask 5 งาน

**6 ส.ค. 2026 (บ่าย) — 4 อย่างที่ทำให้ใกล้ AI ตัวเต็มขึ้น**

เจ้าของถามว่า *"คิดว่าตัวเองมีอะไรอีกมั้ย ที่ทำได้ไม่ดีพอ ที่ต้องเพิ่มให้มันเก่งเหมือน Claude แชท"*
แล้วสั่งให้ทำทุกข้อ

**0. ก่อนอื่น: ความรู้เรื่องตัวเองของมันเป็นเท็จ (บั๊กที่ผมทำไว้เอง)**
`selfStatus()` grep คำว่า "ยังค้าง / ยังไม่ได้ทำ" จากไฟล์บันทึก แล้วเอามาตอบว่าตัวเองขาดอะไร
ปัญหาคือบรรทัดเก่าไม่หายไปตอนงานเสร็จ → มันตอบเจ้าของอย่างมั่นใจว่า
*"แคปหน้าต่างข้าม Space ยังทำไม่ได้จริง"* ทั้งที่ทำเสร็จและเทสผ่านไปแล้ว (commit 8eec518)
จาก 10 บรรทัดที่หยิบไป **3 อย่างทำเสร็จแล้ว** และมีบรรทัดที่อธิบายตัวเองปนมาด้วย

→ `kiki-selfcheck.ts`: ทะเบียนช่องว่าง 11 ข้อ **แต่ละข้อมีวิธีพิสูจน์ของตัวเอง**
ตรวจสดทุกครั้ง · พิสูจน์ไม่ได้ต้องตอบว่า "ไม่รู้" ห้ามเดาว่าเสร็จหรือไม่เสร็จ

*แล้วก็เจอ false positive ซ้ำรอยเดิมทันทีตอนเทส:* ตัวตรวจ OCR หาคำว่า "ocr" ในไฟล์แล้วเจอ
— แต่มันคือข้อความเตือนว่า *"แนะนำให้เปิด OCR"* ซึ่งแปลว่า **ไม่มี** → แก้เป็นเช็คว่ามีโค้ดที่ทำงานจริง
(`tesseract` / `inline_data` / `createWorker`) ไม่ใช่คำในสตริง

**1. ตรวจคำตอบตัวเองก่อนส่ง (`kiki-review.ts`)**
ตัวเต็ม = ร่าง → ตรวจ → แก้ → ส่ง · ของเดิม = เขียน → ส่ง (ไม่เคยอ่านทวนเลยสักครั้ง)
ด่านนี้ตรวจแค่ 2 อย่าง: **ตอบครบไหม** กับ **เคลมเกินหลักฐานไหม** ไม่แตะสำนวน (นั่นเป็นของ persona)
ล้มเหลว/ช้า = ปล่อยของเดิมผ่าน — ด่านตรวจห้ามกลายเป็นจุดพัง
ตัวแก้ที่สั้นกว่าของเดิมเกิน 40% = ไม่ใช้ (น่าจะตัดเนื้อหาทิ้ง ไม่ใช่แก้)

**2. รอบตามเก็บที่เรียกตัวจัดการจริงได้ (`runFollowUp`)**
รากของ "สั่ง 3 อย่างได้คำตอบเรื่องเดียว": ทะเบียนเดินบนลงล่าง ตัวไหนรับก่อนก็จบ
แล้วกฎข้อ 6 ที่เพิ่งเพิ่ม (คำสั่งหลายเรื่อง → chat) ยิ่งทำให้เลี่ยงตัวจัดการที่ลงมือทำจริง
→ รอบตามเก็บ **เดินทะเบียนใหม่** ด้วยข้อความของส่วนที่ตกไป ไม่ใช่แค่ไปหาข้อมูลมาเล่า
เก็บคำตอบไว้ต่อท้าย ไม่ให้ตัวจัดการย่อยส่งของออกเอง · รอบเดียว มี `inFollowUp` กันวนซ้อน

*กับดักที่เจอตอนเทส:* ด่านตรวจเดิมมองหาข้อความ "ก้อนแรกที่ไม่ใช่การ์ด" — ตัวจัดการที่ตอบเป็น
การ์ด HTML เลยทำให้ข้ามการตรวจทั้งอัน ("จดไว้ว่าโทรหาช่างแอร์ แล้วบอกว่าวันนี้มีนัดอะไร"
→ ได้แต่การ์ดงาน เรื่องนัดหายเงียบ) → เปลี่ยนเป็นตรวจจากข้อความรวมทุกก้อน
และต่อส่วนที่ตกเป็นก้อนใหม่เสมอ ไม่ยัดรวมกับการ์ด (จะพัง HTML)

**3. เครื่องมือที่ลงมือทำได้ (`ACT_TOOLS`)**
เดิมลูปมีแต่เครื่องมืออ่าน — ทำวงจร ลอง → เห็นผล → แก้ → ลองใหม่ ไม่ได้เลย
เพิ่ม 3 ตัว: จดงาน · จำข้อเท็จจริง · เก็บลิงก์เข้าคลัง
**ทุกตัวย้อนกลับได้ และตรวจผลเองก่อนคืน** — จดแล้วต้องอ่านกระดานกลับมาว่าเจอจริงไหม
ของที่ยัง **ห้ามอยู่ในลูปเด็ดขาด**: ส่งข้อความแทนเจ้าของ · โพสต์โซเชียล · แก้โค้ดตัวเอง · ลบข้อมูล
(ถอนไม่ได้ → ต้องผ่านขั้นยืนยันของตัวจัดการเดิมเท่านั้น)

**เทสจริง**
- ถามว่าตัวเองขาดอะไร → ตอบ 6 ข้อจากที่ตรวจสดได้ **ไม่มีข้อไหนที่ทำเสร็จแล้วถูกพูดว่ายังไม่ได้ทำ**
  และแยกเองว่าข้อไหน "ไม่ใช่งานพัฒนา แต่ขาดข้อมูลจากโด้" ข้อไหน "ต้องพัฒนาจริง"
- สั่ง 2 เรื่องในข้อความเดียว → งานลงกระดานจริง + ตอบเรื่องนัด (ก้อนที่สองมาจากรอบตามเก็บ)
- ระหว่างทางที่ยังทำไม่ได้ มันรายงานตรง ๆ ว่า *"ระบบตีกลับมาว่าลงกระดานไม่สำเร็จ ผมไม่โม้ว่าจดแล้ว"*
  — ตัวตรวจผลของเครื่องมือทำงานถูกต้องแม้ตอนที่ของข้างล่างพัง

**6 ส.ค. 2026 (เที่ยง) — regex เลือกช่วงวัน: บั๊กเดิมที่ซ่อนอยู่อีกที่**

เจ้าของถาม *"แล้ววันที่ผมไปดูหนัง ผมไม่ได้ลงค่าอะไรบ้าง"* → ตอบ *"วันนี้ยังไม่มีรายการเลยครับ"*
ถามซ้ำด้วยคำหยาบก็ได้คำตอบเดิมเป๊ะ เจ้าของบอกว่า *"ไม่ไหวว่ะ โง่ชิปหาย"*

**ต้นตอ:** `financeItemizeHandler` เลือกช่วงด้วย regex 4 แบบตายตัว
(วันนี้ · เมื่อวาน · สัปดาห์ · เดือน) — **อย่างอื่นตกเป็น "วันนี้" ทั้งหมด**
คือของที่กติกาข้อ 1 ห้ามไว้ตั้งแต่แรก แต่ยังหลงเหลืออยู่ในเส้นทางนี้มาตลอด

**และด่านตรวจก่อนส่งที่เพิ่งทำเมื่อเช้าก็จับไม่ได้** เพราะตั้งเกณฑ์ข้ามคำตอบสั้นกว่า 40 ตัวอักษร
ส่วน *"วันนี้ยังไม่มีรายการเลยครับ"* = 27 ตัว → หลุดด่านไปทั้งสองรอบ
**คำตอบสั้นคือจุดที่ "ตอบไม่ตรงคำถาม" บ่อยที่สุด** → ลดเกณฑ์เหลือ 8 ตัวอักษร

**ที่ทำ**
- `kiki-when.ts` — หาช่วงวันจากภาษาคนโดยอ้าง "เหตุการณ์จริง" ได้
  ดูรายการเงิน 60 วัน + นัดในปฏิทิน แล้วจับคู่กับสิ่งที่เจ้าของพูดถึง
  **หาไม่เจอ = บอกตรง ๆ ห้ามเดาเป็นวันนี้** (นี่คือจุดที่พังเดิม)
  อ่าน `replyText` ด้วย — เจ้าของมัก reply ข้อความที่ระบุวันไว้แล้วแล้วสั่งต่อ
- `itemizedRange()` — ลิสต์ตามช่วงวันอิสระ (ของเดิมรับได้แค่ 4 ช่วง)
- ถามว่า "ไม่ได้ลงอะไรบ้าง" = คิดให้ ไม่ใช่ดัมป์ลิสต์ — เทียบกิจกรรมวันนั้นกับที่ลงไว้แล้วชี้ของที่หาย
- วันที่ตีความได้ไม่มีรายการ แต่วันข้างเคียงมี → บอกด้วย
  (นัดดูหนังลงปฏิทิน 31 ก.ค. แต่**เงินออกจริงวันที่ 1 ส.ค. ทั้งหมด** — ตรวจฐานข้อมูลแล้วยืนยัน
   31 ก.ค. ไม่มีรายการเลยจริง ๆ แปลว่าคำตอบเดิมของ Vex ที่เคยลิสต์ 8 รายการว่าเป็นของ 31 ก.ค. **ผิด**)

**เทสจริง — ผลที่ได้ดีกว่าที่สั่ง**
ถาม "วันที่ไปดูหนังไม่ได้ลงค่าอะไรบ้าง" → หาวันเจอเอง (1 ส.ค. จากรายการตั๋ว SF Cinema)
ลิสต์ที่ลงแล้ว → ชี้ค่าจอดรถที่หายไป → **แล้วจับได้เองว่าตั๋วหารครึ่ง (275 จาก 550)
แต่ป๊อบคอร์น 150 กับ SF 319 ลงเต็มจำนวน ถ้าหารครึ่งด้วยจะผิดไป 234.50 บาท**

**อีก 2 อาการที่เจอระหว่างเทสรอบตามเก็บ (แก้แล้ว)**
- ตัวจัดการย่อยที่ทำงานไม่ได้คืนประโยคเดียว ("จับคู่รายการไม่ได้เลยครับ") ไปต่อท้ายคำตอบที่ดีอยู่แล้ว
- รอบตามเก็บวิ่งไปเจอตัวจัดการเดิม แล้วได้ลิสต์เดิมทั้งก้อน → **เจ้าของเห็นคำตอบเดียวกันสองรอบ**
→ ต่อท้ายเฉพาะของที่ยาวพอและไม่ซ้ำกับของเดิม

**6 ส.ค. 2026 (บ่าย) — คำตอบยาวถูกตัดทิ้ง + เสียงได้ยินแค่ท่อนต้น**

เจ้าของเจอเอง: คำตอบแผนเที่ยวสิงคโปร์จบกลางคำ *"1. **ล็อกอิน Telegram ของโด้ใ"*

**ตัดข้อความ:** ตัวจัดการ 7 ไฟล์ทำ `answer.slice(0, 3900)` — ส่วนที่เกินหายเงียบ
ไม่มีใครรู้ว่าตกอะไรไป (เจ้าของเห็นแค่ประโยคขาดกลางคัน)
→ เลิกตัดที่ปลายทาง ให้ `explodeTextSend` **ซอยเป็นหลายข้อความที่ขอบบรรทัด** แทน
กฎ 3 บับเบิลยอมให้เกินได้ตรงนี้ — **ข้อมูลหายแย่กว่าบับเบิลเยอะ**
บรรทัดเดียวที่ยาวเกินเพดานจริง ๆ (ตาราง/ลิงก์ยาว) จำใจตัดกลาง แต่ไม่ทิ้ง

*เทส:* คำตอบ 6,008 ตัวอักษร → ซอยเป็น 4 ก้อน (152 / 334 / 3,734 / 1,788) จบประโยคครบ
เดิมจะเหลือ 3,900 หายไป 2,100 ตัวอักษร

**เสียงได้ยินแค่ท่อนต้น:** `ttsOgg` ตั้ง `maxChars = 900` แล้ว `speak()` ทำ `.slice()` ดื้อ ๆ
ยาวแค่ไหนก็พูดแค่ 900 ตัวแรกแล้วเงียบ
→ ขยายเพดานเป็น 2,600 และ **ยาวเกินเพดาน = ย่อเป็นคำพูดที่ครบประเด็น ไม่ใช่ตัดครึ่งประโยค**
(คงตัวเลข/ราคา/วันเวลา/ชื่อสถานที่ · ตัดสัญลักษณ์กับลิงก์ออก · ห้ามสรุปจนตกหัวข้อ)
ย่อไม่สำเร็จค่อยถอยไปตัดแบบเดิม — ยังดีกว่าไม่มีเสียง

*เทสทั้งสองทาง:* 1,770 ตัวอักษร (ไม่เกินเพดาน) → 457 KB · 3,152 ตัวอักษร (ต้องย่อ) → 279 KB

**6 ส.ค. 2026 (บ่ายแก่) — เทคนิคการตอบจากตัวอย่างที่เจ้าของส่งมา + เลิกพูดซ้ำ**

**1. เจ้าของส่งภาพบทวิเคราะห์การเงินฉบับเต็มมา 7 รูป** แล้วบอกว่า
*"เวลาผมถามหรือบอกเรื่องอะไรก็ตาม ผมอยากให้มันใช้เทคนิคแบบนี้เลย"*

ถอดเทคนิคออกมา 12 ข้อจากตัวอย่างนั้นทีละอย่าง (ไม่ใช่หลักการทั่วไป — ทุกข้อมีต้นแบบในภาพ)
ฝังเป็น `DEEP_STYLE` ใน `chat.ts`:
- บรรทัดแรกคือ **คำตัดสิน** ไม่ใช่คำเกริ่น ("มีทั้งข่าวดีและระเบิดเวลา 1 ลูก")
- ตัวเลขเรียงเป็นแถว "ชื่อ — จำนวน" ปิดด้วยบรรทัดรวม (Telegram ไม่มีตาราง)
- คำนวณต่อให้เห็นนัย โชว์วิธีคิดให้ตรวจตามได้ ("13,770 ÷ 22,650 = 61%")
- **ตั้งชื่อให้ปัญหา** ("ระเบิดเวลา: พึ่งรายได้เสริมเกินไป")
- **reframe ที่พลิกมุมมอง** ("ไม่ได้มีหนี้เยอะ — สร้างโครงสร้างค่าใช้จ่ายบนรายได้ที่ไม่การันตี")
- ยิงสถานการณ์สมมติให้เห็นความเสี่ยง ("ถ้างาน CRM หายพรุ่งนี้…")
- **จับความไม่สอดคล้องในสิ่งที่เจ้าของบอกเอง** (คมที่สุด ห้ามข้าม)
  "ถ้าเหลือเดือนละ 22,000 ผ่านมา 6 เดือนควรมีเงินเก็บ 135,000 แล้ว แต่บอกว่าเป้าหมายคือให้มีเงินเก็บ
   แปลว่าตัวเลขที่ให้มายังขาดค่าใช้จ่ายจริงไปเยอะ"
- แผนรายเดือนพร้อมผลลัพธ์ต่อขั้น · งานมี "เวลาที่ใช้" กำกับและจำกัดจำนวน
- บอกด้วยว่า **อะไรที่ไม่ควรนับ** · ผูกกับนิสัยจริงของเจ้าของ · ปิดท้ายด้วยข้อมูลที่ยังขาด

และขยายเกณฑ์ `deep` ให้ติดกว้างขึ้น — เดิมแคบจนแทบไม่เคยติด (เจ้าของบ่นว่าตอบตื้น)

**2. พูดซ้ำ** — สอนกฎข้อเดียว ได้ 3 บับเบิลที่พูดเรื่องเดียวกันติดกัน
ที่มาไม่ใช่โมเดลพูดเยอะ แต่เป็นระบบเอาหลายชั้นมาต่อกัน (ตัวจัดการ + รอบตามเก็บ + หมายเหตุ)

**วิธีที่ลองก่อนแล้วใช้ไม่ได้ — จดไว้กันคนหลังพลาดซ้ำ:** วัดความคล้ายของตัวอักษร
(Jaccard บน 3-gram) วัดจริงกับ 3 บับเบิลนั้นได้ **16% / 3% / 2%**
ซึ่งแยกไม่ออกจากข้อความคนละเรื่องที่อยู่ที่ 2-4% — **ภาษาไทยพูดเรื่องเดียวกันด้วยคำคนละชุดได้สนิท**
เกณฑ์เชิงตัวเลขจึงใช้กับเรื่องนี้ไม่ได้เลย

→ `dropRepeats()` ให้สมองตัดสินจากความหมาย · ตัดสินไม่ได้/ล่ม = เก็บทุกก้อน (ห้ามทำข้อมูลหาย)
· กันตัดจนไม่เหลือข้อความ · เรียกเฉพาะตอนมีข้อความธรรมดามากกว่า 1 ก้อน

*เทส:* 3 ก้อนที่พูดซ้ำ → เหลือ 1 · 3 ก้อนคนละเรื่อง (นัด/เงิน/จดงาน) → เหลือครบ 3
