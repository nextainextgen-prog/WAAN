
import { geminiFetch } from "./gemini-usage";/**
 * ตัวจัดเส้นทางคำสั่งของ Vex (เจ้าของสั่ง 4 ส.ค. 2026: "ไม่ต้องฟิกข้อมูลอะไรเลย ปล่อยให้อิสระ")
 *
 * ปัญหาเดิม: ingest ตัดสินใจด้วย regex ล้วน เรียงตายตัว → คำพูดธรรมชาติตกร่องผิดตลอด
 *   เคสจริง: "คิดชื่อกลุ่มนี้ให้หน่อย อยากได้ประมาณว่า..." → ชนคำว่า "อยากได้" เข้าระบบ wishlist
 *            แล้วถามกลับว่า "อยากได้อะไร ราคาเท่าไหร่"
 *
 * โครงใหม่: regex เหลือเฉพาะที่ชัด 100% (ปุ่ม/ยืนยัน/สลิป) ที่เหลือให้สมองอ่านเจตนาแล้วเลือกปลายทางเอง
 * ไม่มั่นใจ = คุยปกติ (ปลอดภัยสุด — ตอบได้เสมอ ดีกว่าลากไปเข้าระบบผิดตัว)
 */

export interface IntentSpec {
  id: string;
  when: string; // อธิบายให้สมองรู้ว่าเมื่อไหร่ควรเลือกอันนี้
  cap?: string; // บรรทัดความสามารถที่จะฉีดเข้า persona (ไม่ใส่ = ไม่ต้องโชว์)
}

// เพิ่มความสามารถใหม่ = เพิ่มที่นี่ที่เดียว (persona อ่านจากตารางนี้ ไม่หลุด sync อีก)
export const INTENT_CATALOG: IntentSpec[] = [
  { id: "chat", when: "คุยเล่น ถามความเห็น ถามความรู้ทั่วไป ปรึกษา หรืออะไรก็ตามที่ไม่เข้าข้ออื่นชัดเจน" },
  {
    id: "think",
    when: "ขอให้คิด/เสนอ/ตั้งชื่อ/ระดมไอเดีย/ช่วยตัดสินใจ/เขียนให้ เช่น 'คิดชื่อกลุ่มให้หน่อย' 'ช่วยร่างข้อความ' 'เสนอแผนมาหน่อย' — ห้ามสับสนกับ wish เด็ดขาด แม้มีคำว่า 'อยากได้'",
    cap: "คิด/เสนอไอเดีย/ตั้งชื่อ/ร่างข้อความให้ (ตอบยาวมีโครงสร้างได้)",
  },

  { id: "finance_record", when: "บอกว่าจ่าย/ซื้อ/ได้เงิน พร้อมจำนวนเงิน หรือส่งสลิปมา", cap: "บันทึกรายรับรายจ่าย (พิมพ์บอกหรือส่งสลิป)" },
  { id: "finance_query", when: "ถามสถานะการเงินรวม เหลือเท่าไหร่ ใช้ไปเท่าไหร่ ขอการ์ดสรุป", cap: "สรุปการเงิน/งบคงเหลือ" },
  { id: "finance_itemize", when: "ขอดูรายการที่ซื้อ/จ่ายไปเป็นรายตัว (วันนี้/เมื่อวาน/สัปดาห์นี้/เดือนนี้)", cap: "ลิสต์รายการใช้จ่ายรายตัว" },
  { id: "finance_analyze", when: "ถามวิเคราะห์ตัวเลข เช่น หมวดไหนเปลืองสุด เทียบเดือน เฉลี่ยต่อวัน ใช้กับอะไรไปเท่าไหร่", cap: "วิเคราะห์การเงินตามคำถาม" },
  { id: "finance_edit", when: "สั่งแก้/ลบ/ปรับตัวเลขรายการเงินที่บันทึกไปแล้ว รวมถึงลบตัวซ้ำ", cap: "แก้/ลบรายการเงินด้วยภาษาคน" },
  { id: "finance_pending", when: "ถามถึง/ขอลิสต์รายการเงินที่ยังไม่รู้ว่าค่าอะไร (รอระบุ) หรือกำลังตอบว่ารายการนั้นคือค่าอะไร", cap: "ลิสต์รายการเงินที่ยังไม่ระบุหมวด + ตอบเป็นชุดได้" },
  { id: "finance_budget", when: "ตั้ง/แก้งบประจำเดือน", cap: "ตั้งงบรายเดือน" },
  {
    id: "finance_baseline",
    when: `บอก/ขอดู/แก้ "ข้อมูลการเงินหลัก" ที่เป็นตัวเลขตั้งต้นถาวร — รายรับจริงต่อเดือน · รายจ่ายที่ตัดประจำทุกเดือน · เงินเก็บที่มีอยู่ · หนี้ค้าง
เช่น "เงินเข้าเดือนละ 45000" · "มีเงินเก็บอยู่แปดหมื่น" · "ผ่อนรถ 7500 ทุกวันที่ 5" · "ขอดูฐานการเงินหน่อย" · "ไม่มีหนี้แล้ว"
ต่างจาก finance_record ตรงที่อันนั้นคือรายการที่เกิดขึ้นครั้งเดียว ส่วนอันนี้คือตัวเลขประจำที่ใช้เป็นฐานวิเคราะห์`,
    cap: "เก็บฐานการเงินหลัก 4 อย่าง (รายรับต่อเดือน · ตัดประจำ · เงินเก็บ · หนี้ค้าง) ไว้ใช้วิเคราะห์แทนการเดาจากยอดไม่กี่วัน",
  },
  { id: "finance_health", when: "ขอดูสุขภาพการเงินภาพรวม ระยะยาว อัตราการออม", cap: "การ์ดสุขภาพการเงิน" },
  { id: "finance_forecast", when: "ถามว่าเงินจะพอไหม คาดการณ์ 30 วัน เส้นเงินสด หรือบอกยอดเงินคงเหลือในบัญชี", cap: "พยากรณ์เงินสด 30 วัน" },
  { id: "bill", when: "เรื่องบิลประจำ/subscription ที่ตัดทุกเดือน", cap: "บิลประจำ/subscription" },
  { id: "debt", when: "เรื่องหนี้ เงินยืม ผ่อน ทวงหนี้ ใครติดเงินใคร", cap: "สมุดหนี้/ผ่อน" },
  { id: "wish", when: "อยากซื้อของชิ้นหนึ่งจริง ๆ แล้วอยากรู้ว่าซื้อไหวไหม/เก็บเข้า wishlist (ต้องเป็นสินค้าที่ซื้อด้วยเงินเท่านั้น)", cap: "wishlist + วิเคราะห์ว่าซื้อไหวไหม" },

  {
    id: "finance_advice",
    when: "เจ้าของเล่ารายละเอียดการเงินของตัวเอง (รายได้ ภาระ หนี้ เป้าหมาย) แล้วอยากให้วิเคราะห์/วางแผน/จัดการให้ — ข้อความมักยาวและมีหลายตัวเลข",
    cap: "ที่ปรึกษาการเงิน: วิเคราะห์โหด + วางแผน + จำข้อมูลการเงิน + ลงงานที่ต้องทำให้อัตโนมัติ",
  },
  { id: "calendar_create", when: "ลงนัด/ลงคิว/นัดหมายที่มีวันเวลาชัดเจน", cap: "ลงนัดเข้า Google Calendar + เตือนเป็นระยะ" },
  { id: "calendar_view", when: "ขอดูตาราง/นัดวันนี้ พรุ่งนี้ สัปดาห์นี้", cap: "ดูตารางนัด" },
  { id: "calendar_edit", when: "เลื่อน/ยกเลิก/ปิดนัดที่ลงไว้แล้ว", cap: "เลื่อน/ยกเลิกนัด" },
  { id: "recurring", when: "ให้เตือนซ้ำเป็นประจำ ทุกวัน/ทุกสัปดาห์/ทุกวันที่เท่าไหร่", cap: "เตือนซ้ำประจำ" },

  {
    id: "task_add",
    when: "สั่งให้จดเป็นงานที่ต้องทำ: 'จดไว้', 'โน้ตไว้', 'เดี๋ยวทำ', 'ลิสต์ไว้', 'เก็บไว้พัฒนา', 'อย่าลืมเตือนผมเรื่อง...' รวมถึงงานมีเงื่อนไข 'ถ้า X แล้วบอกผมด้วย' (ไม่มีวันเวลาชัด = งาน ไม่ใช่นัด)",
    cap: "กระดานงาน: จดสิ่งที่ต้องทำ + ตามเตือนจนกว่าจะปิด (รับงานมีเงื่อนไข 'ถ้า...แล้วเตือน')",
  },
  { id: "task_list", when: "ขอดูงานที่ค้าง/ลิสต์ที่จดไว้/มีอะไรต้องทำบ้าง", cap: "ดูงานค้างทั้งหมด" },
  { id: "task_done", when: "บอกว่างานนั้นทำเสร็จแล้ว/ปิดงาน/ไม่ต้องเตือนแล้ว", cap: "ปิดงานในกระดาน" },

  { id: "memory_remember", when: "สั่งให้จำข้อเท็จจริงเกี่ยวกับตัวเอง/คนรอบตัว/ของสำคัญ (ไม่ใช่งานที่ต้องทำ)", cap: "จำข้อมูลถาวร" },
  { id: "memory_forget", when: "สั่งให้ลืม/ลบความจำเรื่องหนึ่ง", cap: "ลืมเรื่องที่สั่ง" },
  { id: "memory_list", when: "ถามว่าจำอะไรได้บ้าง รู้อะไรเกี่ยวกับเขาบ้าง", cap: "ลิสต์สิ่งที่จำไว้" },
  { id: "memory_recall", when: "ถามถึงเรื่องที่เคยคุยกันไปแล้ว 'จำได้ไหมที่คุยเรื่อง...' 'ที่บอกไว้วันก่อน' 'สรุปที่คุยกันเมื่อวาน'", cap: "ค้นบทสนทนาเก่าย้อนหลังทั้งหมด" },
  { id: "rule_teach", when: "สอน/สั่งให้ปรับพฤติกรรมถาวร 'ต่อไปนี้...' 'อย่า...อีก' 'ครั้งหน้าให้...'", cap: "สอนกฎถาวรให้ Vex" },
  { id: "rule_list", when: "ถามว่ามีกฎอะไรที่สอนไว้บ้าง", cap: "ดูกฎที่สอนไว้" },

  { id: "link_save", when: "ส่งลิงก์มาแล้วสั่งให้เก็บเข้าคลังความรู้", cap: "เก็บลิงก์เข้าคลังความรู้ (เก็บเฉพาะที่สั่ง)" },
  {
    id: "doc_summary",
    when: "สั่งทำ 'เอกสาร/ไฟล์' สรุป — มีคำว่า ทำเป็นไฟล์ / เป็นเอกสาร / ส่งไฟล์มา / HTML / รายงานเป็นไฟล์ (ถ้าขอเป็นไฟล์ ให้เลือกอันนี้ก่อนเสมอ แม้เนื้อหาจะเป็นเรื่องเงินหรือเรื่องอื่น)",
    cap: "ทำไฟล์สรุป HTML ส่งในแชท",
  },
  { id: "image_save", when: "ส่งรูปมาแล้วสั่งให้เก็บไว้", cap: "เก็บรูปเข้าคลัง (เฉพาะที่สั่ง)" },
  { id: "image_find", when: "ขอรูป/ภาพที่เคยเก็บไว้กลับมา", cap: "ค้นรูปที่เก็บไว้" },
  { id: "web_research", when: "ให้หาข้อมูล/ข่าว/ราคา/คอร์ส จากอินเทอร์เน็ตสด ๆ", cap: "ค้นเว็บสด + สรุป" },
  { id: "shopping", when: "ให้หาสินค้าจริงพร้อมราคาและลิงก์ร้าน", cap: "หาสินค้า+ราคา+ลิงก์ พร้อมฟันธง" },

  { id: "tg_group_post", when: "สั่งให้ไปโพสต์/แจ้ง/ประกาศข้อความใน \"กลุ่ม Telegram\" ที่ Vex ประจำการ (ต้องพูดถึงกลุ่ม Telegram ชัด ๆ เท่านั้น — ถ้าพูดถึงห้อง/ช่องในภาพหน้าจอของแอปอื่น ให้ใช้ gui_type)", cap: "โพสต์ข้อความเข้ากลุ่ม Telegram ที่ประจำการ" },
  { id: "tg_dm", when: "สั่งให้ทัก/ส่งข้อความหาคนอื่นในนามบัญชีเจ้าของ", cap: "ส่งข้อความหาคนอื่นในนามเจ้าของ (ยืนยันก่อนส่ง)" },
  { id: "tg_list_chats", when: "ขอรายชื่อแชทในบัญชี Telegram", cap: "ลิสต์แชทในบัญชีเจ้าของ" },
  { id: "tg_chat_summary", when: "ขอสรุปแชท/กลุ่มไหนสักอัน", cap: "สรุปแชทกับใครก็ได้" },
  { id: "tg_create_group", when: "สั่งสร้างกลุ่ม Telegram ใหม่", cap: "สร้างกลุ่ม Telegram ใหม่" },

  {
    id: "social_reply",
    when: "สั่งให้ตอบ/คอมเมนต์โพสต์บนโซเชียล (X/Facebook/Instagram) — มักมีลิงก์โพสต์มาด้วย หรือ reply ข้อความที่มีลิงก์",
    cap: "ตอบ/คอมเมนต์โพสต์โซเชียลแทนเจ้าของ (พิมพ์ค้าง + แคปยืนยัน แล้วรอกดส่ง)",
  },
  { id: "social_post", when: "สั่งให้โพสต์ข้อความใหม่ลงโซเชียล (ส่วนใหญ่คือ X)", cap: "โพสต์ใหม่ลง X (พิมพ์ค้าง + แคปยืนยัน แล้วรอกดส่ง)" },
  { id: "social_status", when: "ถามว่าเชื่อมโซเชียลไว้ไหม ล็อกอินอยู่หรือเปล่า เปิด Chrome ให้หน่อย", cap: "เช็คสถานะล็อกอิน X/FB/IG ใน Chrome ของเจ้าของ" },
  {
    id: "gui_type",
    when: `สั่งให้ "พิมพ์/ส่งข้อความ" ในแอปบนเครื่องแมค (Discord, Warp, LINE, Telegram เดสก์ท็อป, เบราว์เซอร์ ฯลฯ)
สัญญาณสำคัญ: เจ้าของ reply ภาพหน้าจอที่ Vex แคปมา แล้วบอกว่า "พิมพ์ไปในห้องนี้ / ตอบในนี้ / ส่งข้อความในช่องนี้"
คำว่า "ห้อง/ช่อง/แท็บ" ในบริบทภาพหน้าจอ = ห้องในแอปบนจอ ไม่ใช่กลุ่ม Telegram ของ Vex (เคยพลาดมาแล้ว)`,
    cap: "พิมพ์/ส่งข้อความแทนเจ้าของในแอปบนเครื่อง (Discord/Warp/LINE ฯลฯ) พร้อมแคปหน้าจอยืนยันทุกครั้ง",
  },
  {
    id: "gui_switch",
    when: `สั่งให้ "ดู/เช็ค/เปิดขึ้นมาดู" ห้อง/แท็บ/ช่องในแอปบนเครื่อง โดย **ไม่ต้องพิมพ์อะไรลงไป**
สัญญาณชัด: "ไปดู... หน่อย" · "เช็คให้ที" · "มีอัปเดทมั้ย" · "ถึงไหนแล้ว" · "อ่านให้หน่อยว่าขึ้นอะไร"
เคสจริงที่พลาด 6 ส.ค.: "ไปดูแท็บ Warp พัฒนาเลขา Vex 1 หน่อย มีอัปเดทมั้ย" → ถูกตีเป็นสั่งพิมพ์
แล้วไปพิมพ์ใส่แท็บที่มีงานรันค้างอยู่ · **ถามว่ามีอะไร = อ่าน ไม่ใช่พิมพ์**`,
    cap: "เปิดห้อง/แท็บในแอปบนเครื่องขึ้นมาดูแล้วเล่าให้ฟังว่าบนจอมีอะไร",
  },
  {
    id: "warp_cmd",
    when: `สั่งให้ "รันคำสั่ง/พิมพ์ข้อความ" ใน Warp/เทอร์มินัล — ต้องมีเนื้อคำสั่งหรือข้อความที่จะพิมพ์มาด้วยชัด ๆ
เช่น "พิมพ์คำสั่งนี้ให้หน่อย: npm run build" · "สั่งในแท็บพัฒนาว่าให้ทำต่อ"
**ห้ามเลือกอันนี้ถ้าเจ้าของแค่อยาก "ดู" ว่าจอมีอะไร** (ไปดู/เช็ค/มีอัปเดทมั้ย) นั่นคือ gui_switch`,
    cap: "พิมพ์คำสั่งใน Warp ให้จริง + แคปผลกลับมา",
  },
  {
    id: "file_find",
    when: `สั่งให้ "หาไฟล์ในเครื่อง" แล้วส่งไฟล์นั้นกลับมาให้ เช่น "ไปหาไฟล์นี้ในเครื่องผมแล้วส่งมาให้หน่อย" · "ขอไฟล์ใบเสนอราคาที่ทำเมื่อวาน" · "หา monitor-guide.md ให้ที"
ถ้าเจ้าของ reply ข้อความที่มีชื่อไฟล์อยู่ในนั้น แล้วบอกว่า "ไฟล์นี้" = หมายถึงไฟล์ชื่อนั้น ให้ใส่ชื่อลง args ให้ด้วย`,
    cap: "ค้นไฟล์ในเครื่องแล้วส่งไฟล์นั้นเข้าแชทให้เลย (ไฟล์ความลับไม่ส่ง บอกที่อยู่แทน)",
  },
  { id: "mac", when: "สั่งงานที่เครื่อง Mac แบบอื่น: แคปหน้าจอ เปิดแอป/เว็บ เช็คแบต/ดิสก์ อ่านว่าบนจอมีอะไร", cap: "คุมเครื่อง Mac (แคปจอ/เปิดแอป/เช็คเครื่อง)" },
  { id: "eyes_watch", when: "สั่งให้เฝ้า/เลิกเฝ้าข้อความเข้าจากแชทไหน หรือถามว่าตอนนี้เฝ้าแชทอะไรอยู่ ('เฝ้าแชทอั๋น' 'เลิกเฝ้ากลุ่มงาน' 'ตอนนี้เฝ้าอะไรอยู่')", cap: "เลือกว่าจะให้เฝ้าข้อความเข้าจากแชทไหนบ้าง (ปิดอยู่เป็นค่าเริ่มต้น)" },
  { id: "job_status", when: "ถามว่างานที่สั่งไปถึงไหนแล้ว / ยังทำอะไรค้างอยู่บ้าง / เสร็จยัง / คืบหน้ายังไง", cap: "บอกสถานะงานเบื้องหลังที่กำลังทำอยู่" },
  {
    id: "session_auth",
    when: `เรื่อง "เซสชัน/การล็อกอิน" ของระบบตัวเอง: สั่งให้ไปล็อกอินใหม่ · ต่ออายุเซสชัน · ถามว่าเซสชันไหนหมดบ้าง · ตอบรับใบแจ้งว่า "รันเลย/รันให้หน่อย"
รวมถึงตอนกำลังล็อกอินอยู่แล้วเจ้าของส่งเบอร์โทร/รหัส OTP/รหัส 2FA มาให้กรอก`,
    cap: "เช็ค/ต่ออายุเซสชันที่หมดเอง — รันคำสั่งล็อกอินให้ แล้วถามรหัส OTP ในแชทพร้อมภาพหน้าจอ",
  },
  { id: "hermes", when: "ฝากงานยาวหลายขั้นให้ผู้ช่วยเบื้องหลังทำ", cap: "ฝากงานยาวให้ Hermes ทำเบื้องหลัง (รายงานความคืบหน้าทุก 3 นาที)" },
  {
    id: "self_dev",
    when: `สั่งให้ "ลงมือแก้โค้ด" ของ Vex เอง — ต้องเป็นคำสั่งให้ทำ ไม่ใช่คำถาม
เช่น "พัฒนา: เพิ่ม X" · "ไปแก้บั๊กนี้" · "อัปเกรดตัวเองเลย"`,
    cap: "พัฒนาโค้ดตัวเองได้จริง",
  },
  {
    id: "self_reflect",
    when: `เจ้าของ "ถามความเห็นของ Vex เกี่ยวกับตัว Vex เอง" — เป็นคำถาม ไม่ใช่คำสั่งให้ลงมือ
เช่น "คิดว่าตัวเองมีอะไรที่ยังไม่เก่ง" · "มีอะไรอีกมั้ยที่ทำได้ไม่ดีพอ" · "ประเมินตัวเองหน่อย" ·
"จุดอ่อนของมึงคืออะไร" · "อยากพัฒนาอะไรเพิ่ม" · "เสนอไอเดียพัฒนาตัวเองมา"
เคสจริงที่พัง 6 ส.ค.: ถาม "คิดว่าตัวเองมีอะไรอีกมั้ย ที่คิดว่าตัวเองไม่เก่ง" → ไปโผล่การ์ดการเงิน
และอีกรอบ → ถามกลับว่า "ช่วยระบุสเปกให้ชัดเจนหน่อย" ทั้งที่เจ้าของกำลังถามความเห็นมัน`,
    cap: "ประเมินตัวเองจากของจริง (ความสามารถที่มี · งานที่ยังค้าง · สิ่งที่เพิ่งแก้) แล้วเสนอสิ่งที่ควรพัฒนาต่อ",
  },
  { id: "fitness", when: "เรื่องออกกำลังกาย ยิม น้ำหนัก โปรแกรมเล่น", cap: "โค้ชฟิตเนส + จดบันทึก" },
  { id: "journal", when: "เล่าเรื่องวันนี้/สั่งจดไดอารี่", cap: "ไดอารี่ + อารมณ์ประจำวัน" },
  {
    id: "voice_announce",
    when: "สั่งเรื่อง 'ให้ Vex พูดขึ้นมาเองในสาย' — ให้ประกาศ/เตือน/บอกเรื่องต่าง ๆ ด้วยเสียงตอนอยู่ในห้องเสียง หรือสั่งเลิกประกาศ ('เงียบไว้' 'ไม่ต้องพูดขึ้นมาเอง' 'เตือนผมด้วยเสียงด้วย')",
    cap: "พูดขึ้นมาเองในสายเมื่อมีเรื่องควรรู้ (ค่าเริ่มต้นปิด สั่งเปิดเอง)",
  },
  {
    id: "voice_mode",
    when: `ตั้งค่า "โหมด" เสียงพูดของ Vex: ให้ตอบเป็นเสียงตลอด/เลิกตอบเป็นเสียง/เปลี่ยนเสียงพูดเป็นตัวอื่น — เลือกอันนี้ก่อน rule_teach ถ้าเรื่องที่สั่งคือการพูดออกเสียง แม้ขึ้นต้นด้วย "ต่อไปนี้..." หรือ "อย่า...อีก"
ห้ามเลือกอันนี้ถ้าเจ้าของบอก "ข้อความที่จะให้พูด" มาด้วย — นั่นคือ say_voice`,
    cap: "ตั้งโหมดตอบเป็นเสียง/เปลี่ยนเสียง",
  },
  {
    id: "say_voice",
    when: `สั่งให้ "พูดประโยคนี้" ออกมาเป็นเสียง คือมีเนื้อข้อความที่จะให้พูดมาด้วย
เช่น 'พูดว่า "ผมไม่ได้พูดครับ"' · 'อัดเสียงบอกว่า...' · 'ส่งเสียงมาว่า...' · 'อ่านข้อความนี้ให้ฟังหน่อย'
ต่างจาก voice_mode ตรงที่อันนั้นคือเปิด/ปิดโหมด ส่วนอันนี้คือพูดข้อความหนึ่งครั้ง (เคยพลาด: สั่งให้พูดประโยคหนึ่ง แต่ระบบไปเปิดโหมดเสียงซ้ำสองรอบ)`,
    cap: "พูดประโยคที่สั่งออกมาเป็นเสียงให้เลย",
  },
];

export interface RouteResult {
  intent: string;
  confidence: number;
  args: Record<string, string | boolean | undefined>;
  reason?: string;
}

const CATALOG_TEXT = INTENT_CATALOG.map((i) => `- ${i.id}: ${i.when}`).join("\n");

/** รายการความสามารถจริง (ฉีดเข้า persona — ห้ามให้ Vex ปฏิเสธของที่ตัวเองมี) */
export function capabilityLines(): string {
  return INTENT_CATALOG.filter((i) => i.cap).map((i) => `- ${i.cap}`).join("\n");
}

const ROUTER_SYSTEM = `คุณคือตัวจัดเส้นทางคำสั่งของเลขาส่วนตัว (Vex) อ่านข้อความล่าสุดของเจ้าของแล้วเลือก "ปลายทาง" เดียวที่ตรงเจตนาที่สุด

ปลายทางที่มี:
${CATALOG_TEXT}

กติกาสำคัญ:
1. ดูเจตนาจริง ไม่ใช่คำเดี่ยว ๆ — "อยากได้ชื่อกลุ่มเท่ ๆ" คือ think ไม่ใช่ wish · "หาคำตอบให้หน่อยว่าทำไม..." คือ chat ไม่ใช่ web_research ถ้าไม่ต้องใช้ข้อมูลสด
2. ไม่แน่ใจ / กำกวม / เป็นการคุยต่อเนื่อง = ตอบ chat แล้วให้ confidence ต่ำ (ปลอดภัยกว่าลากผิดทาง)
3. คำสั่ง "จดไว้/โน้ตไว้/เดี๋ยวทำ/อย่าลืมเตือนผม" = task_add เสมอ · มีวันเวลานัดหมายชัด = calendar_create · "จำไว้ว่า<ข้อเท็จจริง>" = memory_remember
4. ถามถึงเรื่องที่เคยคุยกันไปแล้ว = memory_recall
5. ข้อความอาจมีหลายเจตนา — เลือกอันที่เป็น "การกระทำหลัก" ที่เจ้าของอยากได้จริง
6. **ข้อความเดียวที่มี "หลายคำขอ" ซึ่งปลายทางเดียวตอบไม่ครบ = ตอบ chat** (เจ้าของสั่งแก้ 5 ส.ค. 2026)
   เส้นทาง chat เป็นทางเดียวที่มีเครื่องมือครบ (ค้นเว็บ อ่านลิงก์ ดูการเงิน ดูนัด ค้นความจำ)
   และตอบหลายเรื่องจบในครั้งเดียวได้ ปลายทางเฉพาะทางตัวอื่นทำงานของตัวเองแล้วจบทันที
   เคสจริงที่พัง: "อยากได้คีย์บอร์ด งบสองพัน **หาให้หน่อยว่าตัวไหนดี** แล้ว**ดูให้ด้วยว่าซื้อไหวมั้ย**"
   → เลือก wish → ได้แค่บันทึกเข้า wishlist + คำนวณวันออม **ส่วนที่ขอให้หาของหายไปทั้งดุ้น**
   วิธีเช็ค: ถ้าปลายทางที่จะเลือก "ทำครบทุกอย่างที่เจ้าของขอในข้อความนี้ไม่ได้" → chat

ตอบ JSON เท่านั้น:
{"intent":"<id>","confidence":0.0-1.0,"reason":"เหตุผลสั้นมาก","args":{}}

args ใส่เฉพาะที่เกี่ยวข้อง:
- ทุกเจตนา: {"deep":true} — ใส่เมื่อโจทย์นี้ "หนัก" พอที่คนถามคาดหวังการวิเคราะห์เต็มรูปแบบ
  เข้าข่าย: วางแผนการเงิน · แผนคุมงบ · แผนอัปสกิล/เรียนรู้ · ตัดสินใจเรื่องใหญ่ที่มีหลายทางเลือก ·
  ถามว่า "ควรทำยังไงดี" กับเรื่องที่มีผลระยะยาว · ขอให้ประเมิน/เทียบ/วางโรดแมป
  ไม่เข้าข่าย: ทักทาย · สั่งงานสั้น ๆ · ถามข้อมูลตัวเดียว · บันทึกรายการ
  (เจ้าของสั่ง 6 ส.ค. 2026: เจอโจทย์หนักให้กางวิเคราะห์ยาวเองอัตโนมัติ ไม่ต้องรอสั่งว่า "วิเคราะห์ละเอียด")
- chat: {"save_link":true} — ใส่เมื่อเจ้าของสั่งให้ "เก็บลิงก์/ข้อมูลนี้เข้าคลัง/เข้าสมอง" มาด้วย
  (เคสจริงที่พัง 6 ส.ค.: "ฝากโพสต์นี้ด้วย อธิบายละเอียด เสนอไอเดีย พร้อมเก็บเข้าสมอง"
   → ไปทาง chat แล้วอธิบายให้ แต่ไม่ได้เก็บ เพราะไม่มีใครบอกว่าต้องเก็บ)
- task_add: {"title":"งานที่ต้องทำ สั้น ชัด","detail":"รายละเอียดถ้ามี","kind":"todo|idea|waiting","priority":"low|normal|high","trigger":"เงื่อนไขที่ต้องเตือน ถ้าเป็นงานแบบ ถ้า...แล้ว","due":"YYYY-MM-DD ถ้าระบุกำหนดชัด"}
- task_done: {"ref":"งานไหน (เลขข้อ หรือคำในชื่องาน)"}
- memory_remember: {"fact":"ข้อเท็จจริงที่ต้องจำ เขียนให้สมบูรณ์ในตัว"}
- memory_recall: {"query":"เรื่องที่เขาอยากให้นึกออก"}
- voice_announce: {"mode":"on|off"} — on = ให้พูดขึ้นมาเองในสาย · off = เงียบ
- voice_mode: {"mode":"on|off|change"} — on = ให้พูดออกเสียง · off = ให้เลิกพูด ตอบเป็นตัวหนังสือพอ · change = เปลี่ยนเสียงเป็นตัวอื่น
- say_voice: {"say":"ข้อความที่ต้องพูดออกเสียง ตัดคำสั่งอย่าง 'พูดว่า' ออก เอาเฉพาะเนื้อความที่จะให้พูด คงคำพูดของเจ้าของไว้ทุกตัวอักษร"}
- tg_chat_summary: {"peer":"ชื่อคน/กลุ่มที่จะให้สรุป"}
- finance_budget: {"amount":"ตัวเลขงบถ้าระบุ","category":"หมวดถ้าระบุ"}
- gui_type: {"app":"ชื่อแอปบนเครื่อง เช่น Discord, Warp, LINE, Telegram","target":"ห้อง/ช่อง/แท็บ ถ้าระบุ","message":"ข้อความที่จะพิมพ์ (ตัดคำสั่งอย่าง 'พิมพ์ว่า' ออก เอาเฉพาะเนื้อความ)","send":true/false}
  ทั้งสี่ค่านี้สำคัญมาก ต้องพยายามกรอกให้ครบเสมอ — ถ้าเจ้าของ reply ภาพหน้าจอ ให้ดูจากบริบทว่าแอป/ห้องไหน
- gui_switch: {"app":"ชื่อแอป","target":"ห้อง/แท็บที่จะเปิด"}
- file_find: {"file":"ชื่อไฟล์หรือบางส่วนของชื่อ ถ้าอยู่ในข้อความที่ reply ถึงให้ดึงมาจากตรงนั้น"}
- warp_cmd: {"command":"คำสั่งหรือข้อความที่จะพิมพ์ใน Warp","tab":"ชื่อแท็บ ถ้าระบุ"}`;

async function routeWithGemini(prompt: string): Promise<RouteResult | null> {
  const key = process.env.GEMINI_API_KEY?.trim();
  if (!key) return null;
  const call = async (withThinkingOff: boolean) => {
    const res = await geminiFetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${key}`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        system_instruction: { parts: [{ text: ROUTER_SYSTEM }] },
        contents: [{ role: "user", parts: [{ text: prompt }] }],
        generationConfig: {
          temperature: 0,
          responseMimeType: "application/json",
          ...(withThinkingOff ? { thinkingConfig: { thinkingBudget: 0 } } : {}),
        },
      }),
      signal: AbortSignal.timeout(20_000),
    }, "router");
    const j = (await res.json()) as { candidates?: { content?: { parts?: { text?: string }[] } }[]; error?: { message?: string } };
    if (j.error?.message) throw new Error(j.error.message);
    return (j.candidates?.[0]?.content?.parts || []).map((p) => p.text || "").join("").trim();
  };
  let raw = "";
  try {
    raw = await call(true);
  } catch {
    try { raw = await call(false); } catch { return null; }
  }
  return parseRoute(raw);
}

function parseRoute(raw: string): RouteResult | null {
  const m = raw.match(/\{[\s\S]*\}/);
  if (!m) return null;
  try {
    const j = JSON.parse(m[0]) as { intent?: string; confidence?: number; reason?: string; args?: Record<string, string> };
    const intent = String(j.intent || "").trim();
    if (!intent || !INTENT_CATALOG.some((i) => i.id === intent)) return null;
    return {
      intent,
      confidence: typeof j.confidence === "number" ? Math.max(0, Math.min(1, j.confidence)) : 0.5,
      args: j.args || {},
      reason: j.reason,
    };
  } catch {
    return null;
  }
}

/**
 * อ่านเจตนาข้อความ — Gemini flash ก่อน (เร็ว ~1 วิ) ล่มค่อยใช้ Claude CLI
 * พังทั้งคู่ = คุยปกติ (ไม่มีทางทำให้แชทเงียบ)
 */
export async function routeIntent(input: {
  text: string;
  replyText?: string;
  convo?: string;
  hasImages?: boolean;
  hasDocs?: boolean;
  replyIsScreenshot?: boolean;
}): Promise<RouteResult> {
  const text = (input.text || "").trim();
  if (!text) return { intent: "chat", confidence: 0, args: {} };

  const prompt = [
    input.convo ? `บทสนทนาก่อนหน้า (ไว้ตีความว่าพูดถึงอะไรอยู่):\n${input.convo.slice(-2500)}` : "",
    input.replyText ? `เจ้าของกำลัง reply ข้อความนี้:\n"""${input.replyText.slice(0, 600)}"""` : "",
    input.hasImages ? "เจ้าของแนบรูปมาด้วย" : "",
    input.replyIsScreenshot ? "สำคัญ: เจ้าของกำลัง reply \"ภาพหน้าจอเครื่อง\" ที่ Vex แคปมาให้ — คำว่าห้อง/ช่อง/แท็บ/ตรงนี้ หมายถึงสิ่งที่อยู่ในภาพนั้น" : "",
    input.hasDocs ? "เจ้าของแนบไฟล์เอกสารมาด้วย" : "",
    `ข้อความล่าสุดของเจ้าของ:\n"""${text.slice(0, 1500)}"""`,
  ]
    .filter(Boolean)
    .join("\n\n");

  const viaGemini = await routeWithGemini(prompt).catch(() => null);
  if (viaGemini) return viaGemini;

  try {
    const { askExtractor } = await import("./kiki");
    const raw = await askExtractor(prompt, { system: ROUTER_SYSTEM, timeoutMs: 45_000 });
    const parsed = parseRoute(raw);
    if (parsed) return parsed;
  } catch { /* ตกไปคุยปกติ */ }
  return { intent: "chat", confidence: 0, args: {} };
}
